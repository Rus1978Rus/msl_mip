ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: AUTHOR_DECISION_20260717_D-SUBSTITUTION-OP-COMPOSITION
DOCUMENT_TYPE: AUTHOR_DECISION
DECISION_ID: D-SUBSTITUTION-OP-COMPOSITION
STATUS: DRAFT — ждёт финального author decision. Оформлено на РЕАЛЬНОМ раскладе
  (4 реальных вердикта из 5 присланных блоков; НЕ 7). Счёт по каждой op_i — ниже.
DATE: 2026-07-17
AUTHOR: Руслан Малявский (AUTHOR_DECISION_STATUS_AUTHORITY)
PROJECT: MSL/MIP Sign Alphabet
SCOPE: O2b — СОСТАВ канонического набора операций op_i (какие входят по умолчанию,
  под какие мишени, сколько). Продолжение D-SUBSTITUTION-OP (op = набор — закрыто).

РАМКА ПРОЕКТА: система ОПОВЕЩЕНИЯ, не антивирус. Машина свидетель, не судья.
  Вердикт статуса — решение автора, не машины/ревьюеров.

============================================================
РЕШЕНИЕ (кратко)
============================================================
Ядро набора (по счёту 4/4 реальных вердиктов) = ТРИ op_i: ZERO-WIDTH-strip,
TAG-strip, BIDI (в наборе). IDNA — сильный кандидат (3/4, один диссент R3). Полный
class-strip — НЕ в default (4/4 → строгий/тестовый профиль). NFC/NFKC и UTS-39 —
отведены (4/4). Роль BIDI (порождает рёбра или detection-only) и граница zero-width —
ОТКРЫТЫ. Число (O3) — после фиксации состава.

============================================================
ПРОВЕНАНС (расклад конвейера — честно, по raw)
============================================================
Конвейер по CONVEYOR_PACKET_SUBSTITUTION_OP_COMPOSITION_2026-07-17. Перепроверка по
raw (Claude Code, файл RAW_REVIEWS_SUBSTITUTION_OP_COMPOSITION_2026-07-17.md).

ПРИСЛАНО 5 блоков — РЕАЛЬНЫХ op-вердиктов ЧЕТЫРЕ: R1 GPT-5.6 (OpenAI), R2 Gemini,
  R3 UNNAMED (LOW_RELIABILITY — фантом-ссылка, нет семейства; в этот раз факты чисты),
  R4 Qwen. ИСКЛЮЧЁН: R5 = META_ASSESSMENT (предсказывает ревьюеров, не вердикт).
  Семейств названо 3 (OpenAI, Gemini, Qwen). ВНИМАНИЕ: не 7 — если 2 ревьюера ещё
  придут, пересчитать.

СЧЁТ ПО КАЖДОЙ op_i (4 вердикта):
  C4 ZERO-WIDTH ... DEFAULT 4/4 (R1,R2,R3,R4) — ЯДРО.
  C3 TAG .......... DEFAULT 4/4 — ЯДРО.
  C2 BIDI ......... В НАБОРЕ 4/4 — ЯДРО (роль спорна, см. OPEN).
  C1 IDNA ......... DEFAULT 3/4 (R1,R2,R4); РЕЗЕРВ 1/4 (R3 демотировал).
  C5 CLASS-STRIP .. НЕ default 4/4 → строгий/тестовый профиль (резерв).
  C6 UAX-31 ....... DEFAULT 1 (R1) / РЕЗЕРВ 1 (R2) / CUT 2 (R3,R4) — в основном вон.
  C7 NFC/NFKC ..... ОТВЕДЕНЫ 4/4.
  UTS-39 .......... вне набора (3 явно + 1 молч.).
  Размер минимума, названный: R1=5, R2=4, R3=3(+2), R4=4 — мода 4, диапазон 3-5.

R5 (мета, НЕ в счёте) держал позицию «убрать C2/bidi в отдельный framework» —
  единственный за «вынести»; 4/4 вердиктов держат bidi В наборе. Мета также предложил
  OP-X контекстную/позиционную strip.

============================================================
ВЕРИФИКАЦИЯ ПРОГОНОМ (claim ревьюеров проверены, не на слово)
============================================================
scratchpad verify_reviewers_composition.py (unicodedata + stringprep + pinned DI):
  - IDNA UTS-46 (пакет idna) НЕ установлен → per-codepoint статусы R1 (200B ignored,
    200C/D deviation, 200E/F disallowed…) НЕ run-verified — остаются reviewer-claimed.
  - IDNA2003/stringprep (stdlib, прокси) на классе 138: REMOVED(map-to-nothing) = 6
    (все PURE); PROHIBITED(bidi, table C.8) = 13 (7 DIR + 6 DEP); прочее 119. Вывод:
    IDNA ГЕТЕРОГЕННА (remove/prohibit/pass), НЕ чистый strip; как edge-op ТОНКАЯ (~6).
    ⭐ ПОДТВЕРЖДАЕТ находку R1: 13 prohibited = ОШИБКИ, не общий канонический ключ →
    «оба отклонены IDNA» ≠ «взаимозаменяемы» (CO_REJECTED ≠ SUBSTITUTABLE).
  - Qwen OP-1 6-list {00AD,200B,200C,200D,2060,FEFF}: все в классе, gc=Cf, bidi=BN — валиден.
  - Qwen cross-class U+FF0F: NFKC='/'; gc=Po; в классе-138 НЕТ — корректно, вне набора.
  - R1 toNFKC_Casefold: в stdlib не воспроизводится (casefold+NFKC ZWSP не убирает) —
    reviewer-claimed, НЕ run-verified.
  - Бакеты 23/12/97/6 переподтверждены.

============================================================
РЕШЕНИЕ — ЧТО ЗАКРЫТО (на реальном счёте)
============================================================
1. ЯДРО НАБОРА (счёт 4/4) = ZERO-WIDTH-strip (C4) + TAG-strip (C3) + BIDI (C2, в
   наборе). Все три — в default у всех 4 вердиктов. Мишени: наивные санитайзеры /
   LLM-входы / исходный код.
2. IDNA (C1) — в набор по умолчанию (счёт 3/4; диссент R3 = резерв). Мишень: домены.
   ПОМЕТКА: IDNA — versioned pipeline (map+validate), НЕ чистый strip (прогон: 6 remove
   / 13 prohibit); переименовать при финализации в IDNA-processing-profile.
3. ПОЛНЫЙ CLASS-STRIP (C5, OP-2≡OP-3) — НЕ в default (счёт 4/4): полный граф 9453 =
   шум для witness. Держать как СТРОГИЙ/ТЕСТОВЫЙ профиль (резерв).
4. NFC/NFKC (C7) — ОТВЕДЕНЫ (4/4, прогон 0/138): не class-strip op. UTS-39 skeleton —
   вне набора (визуальная ось, не невидимая).
5. РАЗМЕР минимального набора ≈ 4 (мода; {IDNA, bidi, tag, zero-width}).

============================================================
OPEN — НЕ РЕШЕНО (следующие author decision / под-конвейер)
============================================================
O2b-1. РОЛЬ BIDI (главная под-развилка): порождает ли C2 substitution-рёбра?
   - R1: НЕТ — detection-профиль, GENERATES_EDGES=NO; CO_DETECTED ≠ SUBSTITUTABLE;
     нужен типизированный вывод op (STATUS/CANONICAL_KEY/WITNESS/REASON).
   - R2, R4: ДА — bidi даёт рёбра (пары с одинаковым визуальным порядком).
   - R3: detection-marked. R5-мета: убрать bidi вовсе.
   Прогон поддерживает осторожность R1 (co-rejected ≠ substitutable). НЕ решено.
O2b-2. АРХИТЕКТУРА ВЫВОДА op (находка R1, VERIFIED + прогон): типизированный результат,
   чтобы CO_REJECTED/CO_DETECTED не порождали ложный полный граф. Требует author
   decision: принять типизацию как норму набора.
O2b-3. ГРАНИЦА ZERO-WIDTH: 6-list (Qwen {00AD,200B,200C,200D,2060,FEFF}) vs PURE-ядро
   (Gemini) vs PURE/BN=23 (R3). Держать = 4/4, но СПИСОК не сведён.
O2b-4. IDNA: версия (IDNA2003 vs UTS-46/IDNA2008) + семантика (edge из map-to-nothing,
   НЕ из prohibit). UTS-46 НЕ run-verified — проверить (поставить пакет idna).
O2b-5. C6 UAX-31: резерв или cut (счёт 1/1/2). NFKC_Casefold (≠ NFKC) — оставить внутри
   identifier-profile (R1), если C6 сохранится.
O3. ЧИСЛО рёбер per-op_i — ПОСЛЕ фиксации состава и роли bidi.

============================================================
АЛЬТЕРНАТИВЫ — ЖИВЫЕ / ОТВЕРГНУТЫЕ (честно)
============================================================
- «Убрать BIDI в отдельный framework» — только R5 (мета, не вердикт); 4/4 вердиктов
  держат bidi в наборе. Живая как под-развилка (O2b-1), но не перевес.
- C8 TARGET_PIPELINE_REPLAY (R1) — воспроизвести реальный pipeline мишени; резерв,
  high-fidelity. Живой.
- OP-X контекстная/позиционная strip (R5-мета) — не покрыта C1-C7; кандидат в C-OTHER.
- C6 UAX-31 как отдельная op — оспорена (R3/R4: сводится к C4/C5).

============================================================
ГРАНИЦА РЕШЕНИЯ
============================================================
- ЗАКРЫВАЕТ (на счёте): ядро 4/4 = {zero-width, tag, bidi}; IDNA 3/4 в набор; class-
  strip не default (строгий/тест); NFC/NFKC и UTS-39 отведены.
- НЕ закрывает: роль bidi (edges vs detection), типизация вывода op, граница zero-width,
  версия/семантика IDNA, судьба C6, число (O3).
- НЕ пересматривает: op = набор (D-SUBSTITUTION-OP); отношение; форма-гибрид; класс 138.
- НЕ трогает код/карточки в этом файле.

============================================================
FORK_STATUS
============================================================
FORK_STATUS: PARTIAL —
  ЯДРО {zero-width, tag, bidi} 4/4 + IDNA 3/4 + class-strip-как-строгий 4/4 +
    NFC/NFKC/UTS-39 отведены 4/4: CLOSED_BY_AUTHOR_DECISION на реальном счёте (4
    вердикта, НЕ 7; R5 мета исключён).
  РОЛЬ BIDI (edges vs detection) + типизация вывода + граница zero-width + версия/
    семантика IDNA + судьба C6 + число (O3): OPEN — PENDING_AUTHOR_DECISION
    (+ возможен под-конвейер по роли bidi / архитектуре вывода).

============================================================
СВЯЗЬ
============================================================
- conveyor_runs/CONVEYOR_PACKET_SUBSTITUTION_OP_COMPOSITION_2026-07-17.md — пакет;
- conveyor_runs/RAW_REVIEWS_SUBSTITUTION_OP_COMPOSITION_2026-07-17.md — raw (5 блоков);
- foundation_layer/AUTHOR_DECISION_20260717_D-SUBSTITUTION-OP.md — op=набор (закрыто);
- tests/verify_substitution_op.py — прогон фактов (9453, OP-2≡OP-3, NFC/NFKC 0/138);
- RULE_WHEN_CONVEYOR_REQUIRED_v0_1 — роль bidi/архитектура вывода = авторская развилка.

END_OF_AUTHOR_DECISION
