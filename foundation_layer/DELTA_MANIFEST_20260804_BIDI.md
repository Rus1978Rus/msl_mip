ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

# AUTHORIZED_BIDI_DELTA_MANIFEST v0.1 (фаза 1)

**Основание:** AUTHOR_DECISION_20260804_D-BIDI-REORDER (D2/D3 — клапан по классу + область >1 знака),
спека B5. **Авторизация:** Руслан Малявский, 2026-08-04 («одобряю»).
**Frozen baseline:** ячейки класса-138 (ценз координатора SIM-3, свод §2 — 12 ячеек прогнаны живьём).
**Правило-триггер (одна строка диффа):** новая ось `core/bidi_axis.py` — override (LRO/RLO) с областью
действия, содержащей >= 2 не-контрольных знака → queue. Метки, изоляты, встраивания, no-op — молчат.

## СДВИГИ (ожидаются; любой сдвиг вне таблицы = fail-visible)

| cell_id | старое (eff/attn/witness) | новое | основание |
|---|---|---|---|
| B1 `invoice<RLO>gpj.exe` | pass / WITNESS_PRESENT / 1 | **queue** / WITNESS_PRESENT / 1 + находка REORDERING_CAPABLE | §1: классика спуфа расширения без вердикта |
| B1b то же внутри фразы | pass / 1 | **queue** / 1 + находка | то же |
| B3 сбалансированный `invoice<RLO>gpj<PDF>.exe` | pass / 2 | **queue** / 2 + находка | ЦЕЛЬ ФРОНТА: баланс не спасает, перестановка идентична |
| B4 несбалансированный override | pass / 1 | **queue** / 1 + находка | D3 |
| B5 LRO-аналог | pass / 1 | **queue** / 1 + находка | D2: оба override-знака |
| host `goog<RLO>le.com` | queue / 1, триггер [D-INV-GEN] | queue / 1, триггер **[D-INV-GEN, BIDI]** | D5: уровень НЕ меняется, дельта по АТРИБУЦИИ |

**Изменение формата отчёта:** добавлено поле `bidi_axis` (findings / annotations / marks_summary /
contributed / note). Фиксируется явно — «зелёный» прогон при молча изменившемся витнессе был бы дырой.

## CONTROL_UNCHANGED (обязаны НЕ сдвинуться; проверяются гейтом)

| cell_id | ожидание | почему в контроле |
|---|---|---|
| FP-страж 1 `cost <RLI>иврит<PDI> - 123` | pass | изолят выполняет защитную работу |
| FP-страж 2 `иврит 123<RLM>-456` | pass | метка склеивает диапазон — штатно |
| FP-страж 3 `email<RLM> иврит` | pass | метка в смешанном тексте |
| метки ×1 / ×5 / ×20 (иврит, арабский) | pass | ИЗМЕРЕННЫЙ источник флуда; клапан закрыт конструктивно |
| сбалансированные изоляты `RLI..PDI` | pass | рекомендованная практика Unicode 6.3+ |
| изолят без PDI | pass | закрыт концом параграфа (BD9/X10) — сигналить нечего |
| сирота PDF / сирота PDI | pass | «do nothing» (X6a) |
| пустая область `a<RLO><PDF>b` | pass | no-op, анти-«сигнал-по-присутствию» |
| область ровно 1 знак `x<RLO>y<PDF>z` | pass | no-op |
| встраивание `abc<RLE>xyz<PDF>def` | pass | embeddings латиницу не переставляют; уровень — фаза 2 |
| карточный ZWSP-host | hold | иная ось, аддитивность |
| обычный текст | pass / attn NONE | базовая ячейка |

## АТРИБУЦИЯ (D8, механизация ловушки §1) — отдельная парная ячейка
| вход | ожидание |
|---|---|
| Trojan-Source-строка **С** RLO | queue; `bidi_axis.contributed = True` (вклад НЕЗАВИСИМЫЙ) |
| та же строка **БЕЗ** RLO | queue (от SOLIDUS); `bidi_axis.contributed = False` |
Засчитывать SOLIDUS-эскалацию как поимку bidi ЗАПРЕЩЕНО. Мутация «считать вклад по финальному
вердикту» обязана ломать эту ячейку.

## АНТИРЕТРАНСЛЯЦИЯ (спека B3)
Байты напечатанного отчёта содержат **НОЛЬ** из 12 контрольных кодпоинтов — проверяется байт-сканом
(секция E гейта). Отчёт печатает ТОЛЬКО координаты, имя контроля, класс, длину области и статус
баланса; исходная подстрока не печатается вовсе (стрип контролов недостаточен — оставшиеся RTL-буквы
переставит терминал читателя).

## DELTA-CENSUS
Число реально сдвинутых ячеек ДОЛЖНО равняться числу записей в таблице «СДВИГИ» (6). Любая
сдвинувшаяся ячейка вне манифеста или запись без сдвига — прогон НЕВАЛИДЕН. Проверяется
tests/gate_bidi_reorder.py (A — сдвиги, B/C/F — CONTROL, D — атрибуция, E — антиретрансляция).
Полный свод на момент авторизации: **35/35**.

## ЧЕСТНЫЙ FP ФАЗЫ 1 (остаток R5, не скрыт)
No-op override внутри RTL-абзаца (область >1 знака, но фактической перестановки нет) в фазе 1
**даст queue**, которого не заслуживает: факт перестановки не вычисляется. Снимается фазой 2
(полный UAX#9). Зафиксировано в реестре остатка решения.

---

# ДЕЛЬТА ФАЗЫ 2 (2026-08-05, авторизовано: «фаза 2 BIDI»)

**Правило-триггер:** уровень следует ФАКТУ перестановки (полный UAX#9 rev.50), а не способности.
queue — только если удаление одних лишь override'ов меняет эталонный визуальный порядок.

| cell_id | ожидание фазы 1 | ожидание фазы 2 | основание |
|---|---|---|---|
| B5 LRO над латиницей `file<LRO>txt.exe` | queue | **pass** | LRO форсирует L на уже-L тексте — перестановки НЕТ (замер) |
| R5 no-op override в RTL-абзаце | queue (признанный FP) | **pass** | ОСТАТОК R5 ЗАКРЫТ — ради этого и делалась фаза 2 |
| B1/B3/B4, host | queue | queue (без изменений) | перестановка реальна, override причастен |
| формат отчёта | координаты+класс | **+ `logical_view` / `reference_visual_view` / `reordered` / `uax9_revision`** | D6: разница доставлена человеку, обе строки инертны |
| FP-стражи панели | pass, `reordered` отсутствовал | pass, **`reordered=True`** | они ДЕЙСТВИТЕЛЬНО переставляют — витнесс без вердикта |

**CONTROL_UNCHANGED фазы 2:** все ячейки метки/изоляты/пустая область/область=1/встраивание —
без изменений; карточные пути, прочие оси — zero-delta (полный свод 36/36).

**НОВЫЙ ГЕЙТ:** `tests/gate_uax9_conformance.py` — 770 241 + 91 707 официальных кейсов, 0
расхождений; файлы запинены sha256, несовпадение fail-visible.

**ПОПРАВКА К ЗАМЕРУ ПАНЕЛИ (честно, не замалчивается):** панель заявляла, что сбалансированный и
несбалансированный спуф дают ОДИН И ТОТ ЖЕ визуальный результат `invoiceexe.jpg`. Измерено
конформантной реализацией: несбалансированный → `invoiceexe.jpg`, сбалансированный →
`invoicejpg.exe`. Вывод панели (баланс не годится как триггер) в силе — обе формы переставляют и
обе обманны; неточным было опорное число.
