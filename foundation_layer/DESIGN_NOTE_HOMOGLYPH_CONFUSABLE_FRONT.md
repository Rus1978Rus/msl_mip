ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: DESIGN_NOTE_HOMOGLYPH_CONFUSABLE_FRONT
DOCUMENT_TYPE: DESIGN_NOTE / FRONT_OPENING
STATUS: DRAFT — фронт ОТКРЫТ 2026-07-22. Кода нет. Требует СВОЕГО конвейера + author decision
  о РАСШИРЕНИИ ОБЛАСТИ (через r>g-фильтр).
DATE: 2026-07-22
SCOPE: видимые визуальные двойники (homoglyphs / confusables) и нормализационные варианты —
  СЕМЕЙСТВО ВНЕ текущей объявленной области (класс-138 = невидимые Cf∧DI). Название: CONFUSABLE_FRONT.

РАМКА: система ОПОВЕЩЕНИЯ, не антивирус. Машина — свидетель. Последнее слово за человеком.

============================================================
ПРОБЛЕМА (измерена в hole-hunt 2026-07-22)
============================================================
Замер (hole_sweep4.py): в хосте выдают ИСТИННО тихий проход (eff=pass, attn=NONE — вообще ноль
сигнала, даже не witness):
  paypоl.com   (кириллица о U+043E)          → тихий pass
  paypοl.com   (греческая омикрон U+03BF)     → тихий pass
  ｐaypal.com   (fullwidth latin U+FF50)       → тихий pass
  ﬁle.com      (лигатура ﬁ U+FB01, NFKC→fi)   → тихий pass
  раypal.com   (смешанная кириллица)          → тихий pass
  p<U+1D41A>ypal.com (math-bold a, NFKC→a)    → тихий pass
  paypıl.com   (dotless i U+0131)             → тихий pass

Классический IDN-homograph: домен визуально = «paypal.com», байтово другой, ведёт куда угодно.

============================================================
ПОЧЕМУ ЭТО НЕ БАГ, А РЕШЕНИЕ ОБ ОБЛАСТИ
============================================================
- Проект «Алфавит знаков» устроен под НЕВИДИМЫЕ структурные угрозы: класс-138 = Cf∧Default_Ignorable.
  Гомоглифы — ВИДИМЫЕ двойники, ДРУГОЕ семейство (Unicode confusables / TR39 / IDN homograph).
  Система на них никогда и не претендовала → это ЗАЗОР относительно миссии, но ВНЕ объявленного.
- Смежное: combining marks (Mn) — тоже вне класса-138, отмечены Codex-аудитом (full-project-audit).
  Оба — «видимая» родня, разумно объединить в один фронт «не-невидимые обманки».
- ВАЖНО (уже сделано): combining mark БОЛЬШЕ не глушит КАРТОЧНУЮ невидимку (419cae2) — это был
  in-scope баг. Но сам Mn/гомоглиф КАК АТАКА (без со-присутствующей невидимки) остаётся вне области.

============================================================
ЧЕМ ОПАСНО РАСШИРЕНИЕ (почему через r>g + конвейер, не хотфиксом)
============================================================
- FP на честных письменностях: кириллица/греческий/CJK/fullwidth — ЛЕГИТИМНЫ в своих доменах и
  текстах. Наивное «любая не-ASCII буква в хосте → тревога» будит пол-интернета. Нужен предикат
  СМЕШЕНИЯ скриптов / confusable-таблица (Unicode TR39 skeleton), а не «не-латиница = плохо».
- Это СЕМАНТИКА/НОРМАЛИЗАЦИЯ — граница с ядром «структура без смысла». Confusable-skeleton — это
  таблица данных (как класс-138), не словарь смысла, так что вписуемо, но требует дизайна.
- Пересечение с CONTEXT_V2 и с C6 (collapse-предикат): «на что похож collapse/skeleton» — общий
  механизм. Возможно, единый слой «видимая форма ≠ байтовая форма → показать проекцию».

============================================================
ВОЗМОЖНЫЕ НАПРАВЛЕНИЯ (для круга, не решать здесь)
============================================================
- SKELETON-проекция: как P1 (каноническая проекция) показывает collapse невидимок — так же
  показывать confusable-skeleton («выглядит как paypal.com; скрипт-микс: latin+cyrillic»). Чистый
  witness, без оценки, дёшево, в рамке. Вероятно лучший первый шаг (как E1 для token).
- Детектор СМЕШЕНИЯ СКРИПТОВ в одной метке хоста (mixed-script) — сильный структурный сигнал.
- NFKC-fold проекция для fullwidth/лигатур/math-alnum.
- Уровень: скорее ВОЗМОЖНАЯ (queue) + проекция, а не hold — как решилось у token.

============================================================
СТАТУС
============================================================
FORK_STATUS: OPEN — фронт открыт, кода НЕТ, конвейер НЕ запускался. Это РАСШИРЕНИЕ ОБЛАСТИ →
  обязателен author decision через r>g-фильтр ДО дизайна. Приоритет: средний (истинно тихо, но
  вне объявленного; in-scope композиция Mn+невидимка уже закрыта 419cae2).
СВЯЗЬ: hole-hunt-2026-07-22 (замеры) · DESIGN_NOTE_E4_CONTEXT_V2 (смежный корень) ·
  full-project-audit-2026-07-21 (Codex отметил Mn) · P1 canonical projection (модель witness-показа) ·
  FOUNDATION_CONCEPT_PIKETTY_R_G (фильтр расширения).

END_OF_DESIGN_NOTE
