BIDI CONVEYOR LEG 01
REVIEWER: Grok / MODEL_FAMILY: xAI-Grok
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_BIDI_2026-08-04

VERDICT: дизайн верен, LOOPHOLE НЕ подтверждён. НОВАЯ ОСЬ (не достройка D-INV-GEN, не семейство
  «скрытые каналы» — здесь ничего не спрятано). Ловушку §1 принял: Trojan-Source не засчитывать.

POSITIONS:
  V1 MECHANISM  -> E = C(класс контроля) + A(несбалансированность) + D(decision-span);
                   ***B (полный UAX#9) CUT из first-cut***: сравнение «видимое vs логическое» зависит
                   от ПОТРЕБИТЕЛЯ (терминал/браузер/редактор), дорого по r>g и всё равно не гарантирует
                   совпадения с рендером пользователя -> усилитель v2.
                   Сигнал: control ∈ {RLO,LRO,RLE,LRE} AND (unbalanced OR in_decision_span),
                   decision_span ≈ filename/extension/path/host-label (грубая эвристика по соседству
                   '.' / '/' / конца токена, БЕЗ полной семантики файла).
                   Метки LRM/RLM/ALM и сбалансированные изоляты — НЕ сигнал сами по себе.
                   Слои: присутствие = class-138 (witness) · host-разрыв = D-INV-GEN (queue) ·
                   ось = класс+баланс+«решает ли перестановка имя/расширение/путь».
  V2 LEGIT      -> белый список МЕТОК + требование несбалансированности/override-в-decision-span.
                   ***Таблица Bidi_Class из UCD НЕ НУЖНА в first-cut*** — 12 известных кодпоинтов
                   покрывают класс; пин при расширении. [экономия шестого артефакта]
                   Таблица легита: RLM/LRM/ALM в прозе (вкл. RTL) -> pass; сбалансированные изоляты
                   -> pass; сбалансированные embedding+PDF вне decision-span -> pass;
                   override без PDF -> queue; override в filename-like -> queue;
                   override без единой RTL-буквы рядом -> queue (но ТОЛЬКО как усилитель вместе с
                   decision-span, не единственный рубильник — может задеть смешанные документы).
  V3 REVEAL     -> ***НЕ рендерить «глаз видит X» полной UAX#9-строкой***: терминал/браузер
                   потребителя переставит её СНОВА (риск ретрансляции, как в TAG/VS).
                   Показывать: bidi_control + offset, class (override/mark/isolate), balance,
                   span_hint (near_extension/free_text/host), logical_slice (экранированный),
                   note «видимый порядок может отличаться в некоторых рендерерах».
                   Опция v2: две строки в ЭКРАНИРОВАННОМ data-only конверте с явным запретом
                   «вставлять как есть в UI».
  V4 LEVEL      -> queue на опасных паттернах; RLM/LRM/ALM в прозе -> pass; сбалансированные изоляты
                   -> pass; несбалансированный override / override в decision-span -> queue;
                   host + bidi -> queue (уже D-INV-GEN, ось НЕ поднимает в hold); hold НЕ вводить.
                   ***КОЛЕБАНИЕ (открытая развилка):*** одиночный RLO в чистой прозе БЕЗ decision-span
                   -> «queue ИЛИ pass+усиленный witness; предпочтительно queue с низким FP-риском на
                   латинице». Позиция не твёрдая -> вынести автору.
                   Контекст «filename» сейчас ОТСУТСТВУЕТ: first-cut = эвристика span (токен с '.' +
                   bidi control); полноценный filename-context = ОТДЕЛЬНЫЙ фронт, не смешивать.
  V5 BATTERIES  -> class-138 уже даёт witness => любое изменение существующих ячеек ТОЛЬКО через
                   авторизованный delta-манифест. Ячейки: invoice<RLO>gpj.exe -> queue;
                   несбалансированный RLO -> queue; сбалансированный RLO..PDF в прозе -> pass;
                   изоляты -> pass; host goog<RLO>le.com -> queue (регрессия D-INV-GEN = 0);
                   иврит/арабский + RLM x1/x5/x20 -> pass; Trojan-Source: ЗАФИКСИРОВАТЬ, что queue
                   от SOLIDUS, bidi alone не ловит; обычный текст -> pass.
  V6 OSTATOK    -> не закрывается: естественный RTL без контролей (порядок меняет сам скрипт);
                   рендерер-зависимость («видимое» у нас != у потребителя); атака, видимая только в
                   конкретном шрифте/движке; НЕТ модели потребителя. Запрет формулировок
                   «bidi закрыт» / «перестановка закрыта».
  V-OTHER       -> НЕ LOOPHOLE: задача разрешима БЕЗ полной модели потребителя, если цель first-cut —
                   поднять СТРУКТУРНО опасные контроли, а не гарантировать WYSIWYG для всех
                   рендереров. Присутствие != угроза перестановки, иначе снова RTL-флуд.

RANKED: 1) override/unbalanced + decision-span -> queue, метки pass, без полного UAX#9
        2) полный UAX#9 сразу CUT (r>g + рендерер)  3) «любой bidi -> queue» CUT (линейный FP на RTL)
        4) ТОЛЬКО несбалансированность без класса/контекста СЛАБО (***сам Grok: пропускает
           СБАЛАНСИРОВАННЫЙ RLO..PDF внутри имени файла***)  5) оставить pass+witness CUT.

COORD-FLAGS (проверить ЗАМЕРОМ на своде):
  * ЦЕНА decision-span: у нас НЕТ контекста «имя файла». Эвристика «токен с '.' + bidi» = фактически
    любой домен/путь/файл. ИЗМЕРИТЬ FP: сколько легитимных токенов с точкой рядом с override
    встречается; и не пересекается ли эвристика с host-путём (двойной сигнал).
  * САМО-ПРИЗНАННАЯ ДЫРА Grok (ранг 4): сбалансированный RLO..PDF ВНУТРИ имени файла не ловится
    правилом «unbalanced». Его же сигнал спасает только через decision-span => decision-span
    становится НЕСУЩИМ, а не усилителем. ПРОВЕРИТЬ ЗАМЕРОМ на `invoice<RLO>gpj<PDF>.exe`.
  * «override почти не нужен в норме» — утверждение без корпуса. Проверить хотя бы, что наш §1-набор
    легита (иврит/арабский с RLM) не содержит override, и честно назвать корпусную лакуну.
  * V4-КОЛЕБАНИЕ (одиночный RLO в прозе: queue или pass) — открытая развилка, не консенсус.
  * Отказ от таблицы Bidi_Class — проверить, что 12 кодпоинтов действительно исчерпывают класс
    (в UCD Bidi_Control=Yes ровно 12? измерить).
