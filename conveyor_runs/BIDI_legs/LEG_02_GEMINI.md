BIDI CONVEYOR LEG 02
REVIEWER: Gemini / MODEL_FAMILY: Google-Gemini
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_BIDI_2026-08-04

VERDICT: дизайн ВЕРЕН, не LOOPHOLE — «закономерная достройка аналитики ПОВЕРХ присутствия в
  классе-138». all (а)-(ж) PASS по всем осям.

POSITIONS:
  V1 MECHANISM  -> A(несбалансированность) + C(класс знака). ***B (полный UAX#9) CUT*** — «потребует
                   эмуляции сложного рендерера в легковесном сканере», катастрофа для r>g
                   [== Grok, второе семейство подряд].
                   Три класса: метки (LRM/RLM/ALM) безопасны по умолчанию · изоляты (LRI/RLI/FSI/PDI)
                   требуют O(n) СТЕКОВОЙ проверки баланса · переопределения (LRO/RLO/PDF) — самый
                   опасный класс, требуют строгой балансировки, и их наличие ВНЕ RTL-контекста —
                   САМОСТОЯТЕЛЬНАЯ аномалия.
  V2 LEGIT      -> белый список МЕТОК + строгий контроль встраиваний/переопределений [== Grok].
                   BASIS: легитимные арабский/иврит массово опираются на НЕЯВНЫЙ порядок и
                   структурные метки (RLM/LRM для пунктуации) => метки на pass+witness дают НУЛЕВОЙ
                   рост FP на естественных текстах.
                   ***ЖЁСТЧЕ GROK:*** эскалации подлежит (i) любой несбалансированный override/isolate
                   И (ii) ***ЛЮБОЙ override (RLO/LRO) как таковой*** — современная типографика
                   (W3C/Unicode) настоятельно рекомендует ИЗОЛЯТЫ, поэтому жёсткие переопределения
                   «почти всегда признак маскарада». [Grok требовал override + (unbalanced OR
                   decision-span); Gemini снимает второе условие => СНИМАЕТ и колебание Grok в V4]
                   Шестой артефакт (Bidi_Class) НЕ обязателен — 12 кодпоинтов = статический словарь
                   в коде [== Grok].
  V3 REVEAL     -> ***показ «видимой» строки КАТЕГОРИЧЕСКИ ЗАПРЕЩЁН*** (жёстче Grok, который допускал
                   v2-опцию в конверте): генерация «глаз видит X» заставляет инструмент УГАДЫВАТЬ
                   поведение потребителя (терминал/IDE) и создаёт риск инъекции bidi прямо в отчёты
                   мониторинга. Отчёт = только структурные факты: [BIDI_ANOMALY: Unbalanced RLO at
                   offset 7]. Человек увидит расхождение в СВОЕЙ среде, имея точные координаты.
  V4 LEVEL      -> queue для структурных аномалий; паритет D-INV-GEN. КЛЮЧЕВОЙ АРГУМЕНТ: «разрыв
                   имени файла или расширения (спуф) КОНЦЕПТУАЛЬНО РАВЕН разрыву хоста» =>
                   тот же уровень. Одиночный RLM в прозе -> pass. Несбалансированный RLO
                   (Trojan Source) или RLO в имени файла -> queue.
  V5 BATTERIES  -> любая эскалация внутри уже покрытого класса-138 СДВИГАЕТ frozen baseline ->
                   AUTHOR_DECISION_MANIFEST обязателен. Ячейки: иврит с 20xRLM -> pass;
                   invoice<RLO>gpj.exe -> queue; несбалансированный код -> queue.
  V6 OSTATOK    -> ярлыки: **BIDI_BYPASS_RENDERER** и **BIDI_NATURAL_RTL**. Система аппаратно не может
                   и НЕ ДОЛЖНА ловить атаки на естественном поведении двуязычного текста БЕЗ явных
                   управляющих знаков (арабские буквы сами разворачивают соседние цифры/пунктуацию).
                   «Видимый порядок — зона ответственности клиента». Формулировка «bidi закрыт»
                   запрещена.

СКЕЛЕТ РЕАЛИЗАЦИИ (Gemini): 1) выделить 12 bidi-знаков внутри проверок класса-138 в отдельный трекер
  состояния; 2) при обходе поддерживать лёгкий O(n) СТЕК: изоляты (LRI/RLI/FSI закрываются PDI) и
  переопределения/встраивания (LRE/RLE/LRO/RLO закрываются PDF); 3) одиночные метки LRM/LRM/ALM —
  мимо стека (pass); 4) если по завершении токена/строки стек НЕ ПУСТ, ИЛИ найден RLO/LRO вне
  легитимного RTL-экранирования -> effective_action = QUEUE; 5) НЕ генерировать визуальную
  репрезентацию, возвращать только индексы и типы нарушителей; delta-манифест.

CONVERGENCE с LEG_01 (2 семейства): полный UAX#9 CUT из first-cut (r>g + рендерер-зависимость);
  метки = белый список, pass; override = опасный класс; баланс через O(n) стек; шестой артефакт
  не нужен, 12 кодпоинтов достаточно; queue, hold не вводить; delta-манифест обязателен;
  остаток = естественный RTL + рендерер-зависимость; запрет «bidi закрыт».
ДИВЕРГЕНЦИЯ (для автора):
  F1 УСЛОВИЕ НА OVERRIDE: Gemini — ЛЮБОЙ RLO/LRO -> queue (аргумент: изоляты рекомендованы, override
     = маскарад). Grok — override + (unbalanced OR decision-span), и сам колебался про одиночный RLO
     в прозе. Позиция Gemini ПРОЩЕ и снимает нужду в decision-span-эвристике, но цена — FP на
     легитимных override.
  F2 ПОКАЗ РАЗНИЦЫ: Gemini — категорический запрет НАВСЕГДА; Grok — запрет в first-cut, опция v2 в
     экранированном data-only конверте.
  F3 «RLO вне легитимного RTL-экранирования» (п.4 скелета Gemini) — предикат НЕ ОПРЕДЕЛЁН
     операционально: что считать «легитимным RTL-экранированием»? Требует уточнения ИЛИ сводится к
     «любой override -> queue».

COORD-FLAGS (проверить ЗАМЕРОМ на своде):
  * F1 — ГЛАВНЫЙ ЗАМЕР КРУГА: цена правила «любой override -> queue». Нужно измерить, встречается ли
    RLO/LRO в ЛЕГИТИМНОМ тексте (у нас §1-легит содержит только метки RLM/LRM). Если легитимных
    override в нашем корпусе нет — правило Gemini дёшево и снимает decision-span целиком.
  * Утверждение «override вне RTL-контекста = самостоятельная аномалия» — проверить, как определять
    «RTL-контекст» без Bidi_Class-таблицы (нужны ли strong-RTL буквы рядом => может понадобиться
    свойство Bidi_Class ВСЁ-ТАКИ, вопреки обеим легам).
  * Сбалансированный RLO..PDF в имени файла (само-признанная дыра Grok): у Gemini ловится ПРАВИЛОМ
    (ii) «любой override», у Grok — только decision-span. ПРОВЕРИТЬ обоими предикатами на
    invoice<RLO>gpj<PDF>.exe.
  * Стековая проверка «по завершении токена ИЛИ строки» — граница не определена: что считать
    завершением? Замерить на многострочном вводе и на токенах без пробелов.
