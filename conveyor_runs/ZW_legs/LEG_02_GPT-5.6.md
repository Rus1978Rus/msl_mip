ZW-BITS CONVEYOR LEG 02
REVIEWER: GPT-5.6 Thinking / MODEL_FAMILY: OpenAI GPT (ПЕРВОЕ настоящее семейство круга)
RECEIVED: 2026-08-05
PACKET: CONVEYOR_PACKET_ZW_BITS_2026-08-05
NOTE: внешняя сверка UTS#39 (join-control rules), UTS#51 (RGI vs общее множество), UAX#14 (WJ/SHY),
  Unicode 16 core-spec гл.9 (ZWJ=Join_Causing, ZWNJ=Non_Joining), /Public/emoji/16.0/.

VERDICT: APPROVE_WITH_MAJOR_ARCHITECTURAL_PATCHES. LOOPHOLE НЕТ. Новый ПЛАГИН семейства поверх
  общего occurrence-индекса; карточные пути СОХРАНЕНЫ.
ГЛАВНАЯ ПОПРАВКА К ПОСТАНОВКЕ: «полного оракула легитимности здесь НЕТ» — принято, НО из этого НЕ
  следует «только витнесс без вердикта». Разделить два независимых вопроса:
  (1) объяснимо ли КОНКРЕТНОЕ вхождение нормальной функцией? (2) образуют ли ВСЕ вхождения структуру
  двухсимвольного носителя данных?
  Формулы: FUNCTIONAL != NO COVERT CAPACITY · NONFUNCTIONAL != PROVEN MALICIOUS.

POSITIONS:
  V1 MECHANISM  -> E: частичный позиционный оракул + ПО-ПАРНАЯ ПРОЕКЦИЯ НОСИТЕЛЕЙ + абсолютная
                   схемная улика + статус функциональности + свёрнутый витнесс.
                   ***АРХИТЕКТУРНОЕ ЯДРО (сильнее панельного C1): CARRIER STREAM — видимые знаки
                   ПОЛНОСТЬЮ ИГНОРИРУЮТСЯ при построении потока носителей.*** Тогда оба измеренных
                   уклонения §1 становятся структурно НЕРЕЛЕВАНТНЫ, а не «обработаны»:
                   разбавление не меняет поток; прогон=1 не разрушает поток; source-offset'ы хранятся.
                   ***ВСЕ 15 ПАР, не одна выбранная:*** шесть носителей дают 15 неупорядоченных пар,
                   проверяется каждая => добавление ТРЕТЬЕГО zero-width знака не отменяет
                   доминирующий двухсимвольный поток. 15 x O(n) = O(n) с малой константой, один проход.
                   ***ЧЕТЫРЁХСОСТОЯННЫЙ статус функции*** (вместо бинарного легит/атака):
                   PROVEN_FUNCTIONAL / PLAUSIBLE_FUNCTIONAL / NONFUNCTIONAL / UNVERIFIABLE.
                   CUT: плотность как детектор · ***длина прогона как ТРЕБУЕМЫЙ признак*** ·
                   ***ЧЕРЕДОВАНИЕ как ТРЕБУЕМЫЙ битовый паттерн*** · «печатный декод» как улика атаки.
  V2 LEGIT      -> запиненный МУЛЬТИ-ИСТОЧНИКОВЫЙ контекст-оракул, но с ЧЕТЫРЁХСОСТОЯННЫМ выходом.
                   НЕ «правило совпало = абсолютно безопасно» и НЕ «правило не совпало = доказанная
                   атака». Bundle: ArabicShaping + Scripts + ScriptExtensions + **IndicSyllabicCategory**
                   + emoji-zwj-sequences + emoji-test; из них ГЕНЕРИРУЕТСЯ один компактный
                   неизменяемый рантайм-артефакт + манифест хэшей источников (лучше по r>g, чем
                   множество ручных исключений).
                   Пер-носитель: ZWJ в ТОЧНОЙ RGI -> PROVEN; well-formed НЕ-RGI -> PLAUSIBLE (UTS#51
                   различает общее множество и RGI => отсутствие в RGI НЕ доказывает атаку);
                   malformed -> NONFUNCTIONAL. ZWNJ между латинскими -> NONFUNCTIONAL (достаточный
                   структурный сигнал, но НЕ доказательство намерения). WJ -> PLAUSIBLE/UNVERIFIABLE
                   (для полной проверки нужен line-break профиль UAX#14, не только соседи).
                   SHY внутри алфавитного слова -> PLAUSIBLE; после пробела/пунктуации -> NONFUNCTIONAL.
                   ZWSP в ASCII/машинном токене -> NONFUNCTIONAL. BOM внутри текста -> NONFUNCTIONAL_AS_BOM.
                   ГЛАВНАЯ ГРАНИЦА: ORACLE MATCH -> «функциональность МОЖЕТ быть объяснена», НЕ
                   «скрытый бит невозможен».
  V3 REVEAL     -> СХЕМО-НЕЙТРАЛЬНОЕ раскрытие ПЕРВЫМ; опциональный декод ТОЛЬКО по объявленному
                   доверенным вызывающим профилю. Канонический поток: младший кодпоинт = SYMBOL_A,
                   старший = SYMBOL_B, с явной меткой «A/B — КОНВЕНЦИЯ ОТЧЁТА, значение бита
                   НЕИЗВЕСТНО». Битовая строка допустима только с MAPPING_STATUS=REPORTING_CONVENTION.
                   Байтовый декод по умолчанию NOT CLAIMED. Forensic-режим: 2 полярности x 2 порядка
                   x 8 фаз = до 32 гипотез, БЕЗ ранжирования по словам, «валидный UTF-8» НЕ вердикт,
                   полный вывод не идёт в основной отчёт.
  V4 LEVEL      -> PROVEN_FUNCTIONAL -> pass · изолированное PLAUSIBLE/UNVERIFIABLE -> существующий/
                   свёрнутый витнесс · изолированное NONFUNCTIONAL -> витнесс + существующая
                   карточная политика · СИЛЬНЫЙ двоичный кандидат -> MEDIUM/QUEUE · host остаётся
                   существующим queue (ценность — независимая АТРИБУЦИЯ, не повышение) ·
                   HIGH/HOLD не по умолчанию. ОБЪЁМ не поднимает уровень (24/240/3000 бит -> queue).
                   Карточные вердикты НЕ переписываются; final = max, cause-ledger обязателен;
                   ***нельзя засчитывать queue от Input-Guard как успех новой оси***.
                   Свёртка витнесса: 240 отдельных строк НЕ должны быть главным человекочитаемым
                   выходом — одна сводка ZERO_WIDTH_BIT_SUMMARY, occurrence-записи в машинном trace.
  V5 BATTERIES  -> AUTHORIZED_ZW_BIT_DELTA_MANIFEST_v0_1 с полем EXPECTED_CAUSAL_CONTRIBUTOR.
                   Обязательные атаки: 4 пары x {блок 24 бита, размазанная прогон=1, разбавленная
                   100/200/800/1600, реалистичная 240 бит в 2000 знаках}.
                   ***КРИТИЧЕСКИЙ ТЕСТ РАЗБАВЛЕНИЯ:*** тот же carrier stream + разное видимое
                   прикрытие -> ОДИНАКОВАЯ классификация плагина; меняться могут ТОЛЬКО offset'ы,
                   видимая длина и плотность Input-Guard; НЕ должны меняться поток, детект пары,
                   уровень плагина, хэш нагрузки.
                   ***КРИТИЧЕСКИЙ ТЕСТ РАЗМАЗЫВАНИЯ:*** блочный и размазанный потоки с одинаковым
                   порядком носителей -> один канонический поток, один хэш, одна находка.
                   Карточные батареи ZWSP 21/21, ZWJ/BOM 11/11, мутации 5/5 — zero semantic delta.
                   MUTATION MANIFEST MUT-ZWB-01..16 (вкл. MUT-ZWB-12 «засчитать queue от Input-Guard
                   как успех плагина» и MUT-ZWB-14 «240 алертов вместо одной сводки»).
  V6 OSTATOK    -> 9 именованных остатков: один бит на функциональной позиции (функциональное и
                   скрытое использование МОГУТ СОСУЩЕСТВОВАТЬ — принципиально) · presence/absence
                   канал (нет двухсимвольного алфавита — первый фронт его НЕ закрывает) ·
                   один носитель с позиционным кодированием · полностью функциональные позиции ·
                   нагрузка меньше evidence-budget · межсообщенческое разнесение · неизвестная схема
                   (CARRIER DETECTED != MESSAGE RECOVERED) · ***MULTIARY алфавит: шесть носителей
                   дают БОЛЬШЕ одного бита на вхождение (четыре знака = до 2 бит)*** · прочие каналы.
                   Разрешённый статус: «измеренные ДВУХНОСИТЕЛЬНЫЕ потоки — покрыты КАК КАНДИДАТЫ»;
                   запрещённый: «zero-width стеганография закрыта».
  V-OTHER-1     -> НОВЫЙ ПЛАГИН поверх общих occurrence-записей. Карточка отвечает «что делает ОДИН
                   знак в локальном контексте», плагин — «образуют ли МНОГО РАЗНЫХ знаков дальний
                   поток». Нельзя размазывать по-парную логику по пяти карточкам:
                   CROSS-CARD CHANNEL REQUIRES ONE CROSS-CARD PLUGIN.
  V-OTHER-2     -> ЧЕТЫРЁХФАЗНОЕ внедрение. ***ФАЗА 0 — инвентаризация носителей БЕЗ изменения
                   вердикта:*** доказать, что плагин ВИДИТ все измеренные потоки, при полном
                   zero-delta гейте. Затем фаза 1 (сильные нефункциональные кандидаты), фаза 2
                   (WJ/SHY и многоязычные документы — после корпуса), фаза 3 (presence/absence,
                   multiary, cross-message — отдельные фронты).
  V-OTHER-3     -> ***СЕГМЕНТАЦИЯ (никто больше не поднял):*** нельзя безусловно объединять все
                   zero-width вхождения большого документа — ZWNJ из персидского абзаца + WJ из
                   заголовка + SHY из немецкого слова создадут ИСКУССТВЕННУЮ пару. Порядок:
                   доверенное поле -> абзац -> защищённый токен/host -> ограниченный сегмент.
                   Источник границы детерминирован и аудируем; атакующий НЕ выбирает профиль
                   содержимым строки. Дыра: разнесение по границам сегментов — честный остаток.

CONVERGENCE с LEG_01 (панель): плотность как детектор CUT; queue-паритет, hold не вводить, host не
  поднимать (только атрибуция); догадочный байтовый декод не заявлять; свёртка витнесса обязательна;
  карточные вердикты не переписывать; запрет засчитывать чужую эскалацию; «закрыто» запрещено;
  остаток с именами (обе стороны независимо назвали presence/absence и функциональную позицию).
РАСХОЖДЕНИЯ С ПАНЕЛЬЮ (для свода):
  F1 ***ЧЕРЕДОВАНИЕ КАК ПРИЗНАК.*** Панель C1 требует «>= t=4 ПЕРЕКЛЮЧЕНИЙ»; GPT ЯВНО ВЫРЕЗАЕТ
     чередование как требуемый паттерн: произвольные данные дают 00000000 / 11110000, переключений
     мало. ЕСЛИ GPT ПРАВ — предикат панели пропускает нагрузки с длинными сериями одинаковых бит.
     ИЗМЕРИМО -> проверяю замером.
  F2 ОРАКУЛ БИНАРНЫЙ vs ЧЕТЫРЁХСОСТОЯННЫЙ: панель — позиция функциональна/нет; GPT — четыре
     состояния, где «правило не совпало» НЕ равно «доказанная атака».
  F3 СОСТАВ BUNDLE: панель — JT + Scripts + ccc=9(stdlib) + emoji; GPT добавляет
     ScriptExtensions + IndicSyllabicCategory + emoji-test и предлагает ГЕНЕРИРОВАТЬ один артефакт.
  F4 СЕГМЕНТАЦИЯ: только GPT. Панель считает поток по всему входу => на многоязычном документе
     рискует искусственной парой (её же FP-гвард ZW-FP-MIXED-LEGIT это косвенно ловит).
  F5 ФАЗА 0 (инвентаризация без вердикта): только GPT. Дешёвый способ доказать покрытие до дельт.
  F6 MULTIARY: только GPT — шесть носителей дают >1 бита на вхождение; парные проекции этого не
     восстанавливают.

=== ЗАМЕР КООРДИНАТОРА ПО F1 (2026-08-05) — РЕШАЕТ РАСХОЖДЕНИЕ В ПОЛЬЗУ GPT ===
Считал число ПЕРЕКЛЮЧЕНИЙ бита в реальных нагрузках против панельного порога t=4:
  «send all keys» (104 бита) -> 53 переключения -> ловится
  «key» (24 бита)            -> 15 -> ловится
  случайные байты            -> 36 -> ловится
  0x55 x8 (чередование)      -> 63 -> ловится
  0x00 x8 / 0xFF x8          ->  0 -> ***ПРОПУСК*** (но там один символ, C1 отсекает по «>=2 разных»)
СКОНСТРУИРОВАННЫЙ ОБХОД (оба символа присутствуют, порог обойдён):
  240 бит вида 80 нулей + 80 единиц + 80 нулей -> **2 переключения** -> ***ПРОПУСК порогом t=4***,
  при этом нагрузка полноценная (30 байт) и оба носителя реально используются.
ВЫВОД: на ИЗМЕРЕННЫХ образцах порог панели работает, но число переключений — **параметр, который
  выбирает АТАКУЮЩИЙ**, ровно как длина прогона (убита в круге TAG) и плотность (убита здесь, §1).
  Это ТРЕТИЙ подряд управляемый атакующим параметр, и два предыдущих круг уже похоронил замером.
  ⇒ Позиция GPT «чередование НЕ может быть требуемым признаком» подтверждена;
  панельный C1 в текущем виде даёт ложное чувство покрытия.

=== ЗАМЕР КООРДИНАТОРА ПО АРХИТЕКТУРЕ GPT ===
Проверил ключевое утверждение «carrier stream инвариантен к прикрытию и фрагментации»:
  блочная форма   -> поток ABBABABBABBAABABABBBBAAB (24)
  размазанная (по одному носителю между 200 видимыми знаками) -> ТОТ ЖЕ поток, тот же порядок
  ПОТОКИ ИДЕНТИЧНЫ ⇒ оба измеренных уклонения §1 становятся структурно НЕРЕЛЕВАНТНЫМИ,
  а не «обработанными порогом». Это качественно сильнее панельного C1: там инвариантность —
  свойство признака, здесь — свойство САМОГО ПРЕДСТАВЛЕНИЯ.
