TAG CONVEYOR LEG 03
REVIEWER: Qwen (Qwen3) / MODEL_FAMILY: Alibaba
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_W_TAG_2026-08-04

VERDICT: APPROVE_WITH_AMENDMENTS. дизайн верен, LOOPHOLE НЕТ. Ось = сокрытие СОДЕРЖАНИЯ,
  смешивать с W4-VS/W7 было бы ошибкой (там подмена представления, здесь сокрытие содержания).

POSITIONS:
  V1 MECHANISM  -> C (декод как объект) + B (порог >=3 смежных) [== Grok, ПРОТИВ GPT].
                   A отклонён (агрессивно: одиночка может быть артефактом неполной последовательности);
                   D плотность отклонена (§1 обход). Декодер = одноразовая разработка, НЕ версионируемый
                   артефакт. Дыра признана: декодер не отличает осмысленное от мусора -- и это ОК,
                   интерпретация делегируется человеку (witness-рамка).
  V2 LEGIT      -> (i) строгий RGI-шаблон: база U+1F3F4 + регион ∈ ЗАПИНЕННЫЙ список + CANCEL -> молчать;
                   всё прочее -> сигнал. Пограничные (свои ожидания): без базы -> queue; gbxyz -> queue;
                   флаг + хвост после CANCEL -> queue. [== Grok, ПРОТИВ GPT-классификатора]
                   ***ВНУТРЕННЕЕ ПРОТИВОРЕЧИЕ (для свода):*** Qwen называет список "RGI", но описывает
                   его как "~300 субрегионов CLDR / ISO 3166-2" -- это НЕ RGI (RGI = ровно 3), это
                   фактически GPT-шный VALID_NON_RGI по CLDR subdivision. Т.е. размер списка у Qwen
                   косвенно ПОДТВЕРЖДАЕТ поправку GPT, хотя ярлык стоит грок-овский.
  V3 REVEAL     -> (b) усечённо + экранированно (HTML-entities или Unicode-escape); усечение ~50 симв +
                   [truncated]. (a) CUT (XSS в HTML-отчёте), (c)/(d) CUT (witness пуст).
                   [ЕДИНОГЛАСНО 3/3 с Grok и GPT]
  V4 LEVEL      -> проза -> queue; ***host -> HOLD БЕЗУСЛОВНО*** (содержание в host = явная попытка
                   обмана; выше, чем host-РАЗРЫВ у D-INV-GEN, который queue); одиночка -> pass (порог из
                   V1); U+E0001 -> queue. "Осмысленный ASCII" влияет на ПРИОРИТЕТ в witness, НЕ на
                   уровень [частично снимает спор Grok/GPT об эвристике].
                   Дыра признана: легит в host возможен (test<TAG:debug>.com) -> решает человек по witness.
  V5 BATTERIES  -> zero-delta для входов без tag; 10 обязательных ячеек (3 легит-флага, нагрузка в прозе,
                   нагрузка в host, одиночка, РАЗБАВЛЕННАЯ 120+, хвост после CANCEL, gbxyz, unassigned).
                   ***Unassigned -> pass*** ("не в классе-138") [ПРОТИВ GPT: witness/UNVERIFIABLE].
                   Пин: список регионов + версия (назвал CLDR v44 / Unicode 17.0).
  V6 OSTATOK    -> не закрывает: VS-кодирование (W4-смежно), zero-width битовые схемы,
                   ***U+1BCA0-1BCA3 Shorthand Format Controls*** (НОВЫЙ названный канал, не звучал у
                   Grok/GPT) -- отдельные оси. "TAG закрыт" != "сокрытие закрыто"; ложное чувство
                   защищённости лечится документацией.
  V-OTHER       -> LOOPHOLE НЕТ.

RANKED (Qwen): 1) декодер+RGI-шаблон (критично: без него сигнал = побочный эффект плотности)
  2) экранирование раскрытия (иначе отчёт = вектор инъекции)  3) запиненный список  4) ратификация
  уровней и ячеек.

=== СЧЁТ ПО РАЗВИЛКАМ ПОСЛЕ 3 СЕМЕЙСТВ (Grok / GPT / Qwen) ===
  F1 ПОРОГ N:      N>=3 = Grok+Qwen (2)  |  любой tag = GPT (1). НО GPT дал ИЗМЕРИМЫЙ контр-довод
                   (односимвольные разнесённые runs обходят порог) -- проверить СИМОМ, не голосом.
  F2 ОДИНОЧКА:     pass = Grok+Qwen (2)  |  queue = GPT (1). Связано с F1 (порог => одиночка молчит).
  F3 HOST:         hold = Qwen (безусловно) + Grok (условно, при "осмысленном ASCII")  |  queue = GPT
                   (hold только при доп. улике). 2 vs 1 в сторону hold, но с РАЗНЫМ основанием.
  F4 ЛЕГИТ-АРТЕФАКТ: "RGI-список" = Grok+Qwen (2)  |  четырёхсостоянный validity-классификатор = GPT (1).
                   НО Qwen сам описал список как ~300 CLDR-субрегионов => фактически 2.5 из 3 за
                   CLDR-валидность, спор скорее о ЯРЛЫКЕ, чем о существе. GPT технически точен
                   (RGI = ровно 3; правило "молчать на трёх" будит валидные не-RGI флаги).
  F5 UNASSIGNED:   pass = Qwen (1)  |  witness/UNVERIFIABLE без декода = GPT (1)  |  Grok: "не silent".
                   2 из 3 против чистого pass.
  ЕДИНОГЛАСНО (3/3): плотность CUT как детектор; декод обязателен; раскрытие = экранированный усечённый
                   preview; проза = queue; TAG != "сокрытие закрыто"; VS/ZW = отдельные оси; ось как
                   потребитель класса-138, не второй сканер.

COORD-FLAGS (замером на своде):
  * ВЕРСИОННЫЙ КОНФЛИКТ УСИЛИЛСЯ: ВТОРАЯ лега подряд пинит Unicode 17.0 / CLDR v44, а проект запинен на
    unidata 16.0.0 (core/unicode_tables.py assert + PIN_MANIFEST). ИЗМЕРИТЬ, что доступно под 16.0.0.
  * F1/F2 -- измерить ЦЕНУ порога: сколько односимвольных tag-runs нужно, чтобы пронести смысл мимо N>=3
    (GPT-контр-довод), И сколько ложных queue даст "любой одиночный tag" на легит-корпусе.
  * F3 -- измерить, ЕСТЬ ЛИ вообще легит с tag внутри host (Qwen сам привёл test<TAG:debug>.com как
    сомнение); от этого зависит hold vs queue.
  * F4 -- проверить, есть ли CLDR subdivision-данные в проекте/stdlib или это ЧЕТВЁРТЫЙ тяжёлый артефакт;
    сравнить со СТРУКТУРНОЙ формой (база + 2-6 tag-букв/цифр + CANCEL, <=32 cp) по r>g.
  * F5 -- проверить, как unassigned tag-кодпоинты (U+E0000, E0002..E001F) ведут себя СЕЙЧАС (в §1 они
    не мерились отдельно).
