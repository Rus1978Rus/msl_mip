TAG CONVEYOR LEG 01
REVIEWER: Grok / MODEL_FAMILY: xAI-Grok
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_W_TAG_2026-08-04

VERDICT: дизайн верен, LOOPHOLE НЕ подтверждён. Ось = "сокрытое СОДЕРЖАНИЕ", не дубль плотности/W4/W7.

POSITIONS:
  V1 MECHANISM  -> B+C: run смежных TAG >= N (N=3) вне валидного RGI-флага (детект) + ДЕКОД TAG->ASCII
                   (witness + эвристика осмысленности). ДОСТРОЙКА класса-138, НЕ параллельная ось
                   (аналогия W4-VS: класс несёт presence, фронт достраивает семантику носителя).
                   A (любой tag вне флага) СЛАБЕЕ (шум на одиночках); D (плотность) CUT -- обход измерен,
                   это Input-Guard, не свойство фронта.
  V2 LEGIT      -> строгий RGI-шаблон + ЗАПИНЕННЫЙ список регионов:
                   молчать ТОЛЬКО U+1F3F4 + [tag-буквы, регион ∈ pinned RGI, len 5..6] + U+E007F
                   и ничего больше в той же tag-run. Сигнал: run без базового флага / валидный шаблон
                   но регион вне списка (gbxyz) / лишняя нагрузка ПОСЛЕ CANCEL / run в host-прозе.
                   Список мал и стабилен -> r>g окупается (паттерн пина как в W7).
                   Дыра: устаревание списка -> новый субрегион = fail-visible СИГНАЛ, не silent pass.
  V3 REVEAL     -> (b) УСЕЧЁННО + ЭКРАНИРОВАННО. (c)/(d) CUT -- witness-канал пуст (логика W7:
                   раскрытие обязательно для "смотри глазами"); (a) CUT -- сырой decoded в лог/консоль/
                   отчёт = вектор инъекции. Обязательно: экранирование ПОД КАНАЛ вывода; усечение
                   (~80-120 симв + "...") + ПОЛНАЯ ДЛИНА в метаданных; НИКОГДА не исполнять decoded.
  V4 LEVEL      -> валидный RGI-флаг = pass(молчать); tag-run >=N в прозе с осмысленным ASCII = queue;
                   в host (уже queue от D-INV-GEN) -> HOLD только если декод похож на команду/URL/
                   обход-allowlist (эвристика "осмысленный ASCII": доля printable, длина, пробелы/
                   токены -- НЕ языковая модель); одиночный tag = pass + presence в witness;
                   U+E0001 LANGUAGE TAG (deprecated RFC 2482) = лёгкий witness/queue.
  V5 BATTERIES  -> zero-delta на входах без tag; ячейки: легит gbsct/gbwls/gbeng->pass; нагрузка в
                   прозе ВКЛЮЧАЯ прикрытие 120/300/600 -> queue (НЕ pass); нагрузка в host;
                   одиночка->pass+witness; нагрузка ПОСЛЕ CANCEL; невалидный регион gbxyz;
                   unassigned часть блока -> не silent. Пин: RGI-region list + версия.
  V6 OSTATOK    -> НЕ закрывает: VS-encoding (смежно W4), zero-width bit schemes, bidi как носитель
                   данных = ОТДЕЛЬНЫЕ оси; ложное чувство "TAG закрыт != сокрытие закрыто";
                   новые RGI-регионы до refresh = временный сигнал; **СТЕГО ВНУТРИ ВАЛИДНОГО
                   RGI-ШАБЛОНА** (атакующий укладывается в шаблон) = остаток шаблона.
  V-OTHER       -> не LOOPHOLE: уникальное свойство TAG = ПОЛНЫЙ ASCII-алфавит невидимыми буквами;
                   резать как "ещё один class-138" недостаточно (нагрузка не читается).

RANKED: 1) run>=N + декод + RGI-whitelist + queue + усечённое экранированное раскрытие  2) любой tag
        без декода (слабее)  3) только плотность CUT  4) полное неэкранированное раскрытие CUT
        5) не раскрывать содержание CUT.

COORD-FLAGS (проверить ЗАМЕРОМ на своде, не на слово):
  * N=3 -- измерить FP/охват: есть ли легит с 2-3 смежными tag ВНЕ флага? (флаг = 5-6 + CANCEL,
    т.е. N=3 не задевает флаг, но проверить реальные последовательности.)
  * "pinned RGI region list" = ЧЕТВЁРТЫЙ версионируемый артефакт репо (после confusables/Scripts/
    ScriptExt). Проверить цену r>g: возможно, дешевле структурный шаблон БЕЗ списка регионов
    (база+5-6 букв+CANCEL) + сигнал только на отклонение от ФОРМЫ. Grok сам признал устаревание
    списка дырой -- измерить, что даёт список сверх формы.
  * "hold при осмысленном ASCII в host" -- эвристика осмысленности НЕ определена операционально;
    измерить ложные срабатывания на случайных tag-run.
  * Раскрытие: проверить, что print_report/отчёт экранирует decoded (сейчас витнесс печатает
    unicode_name, не нагрузку -- новый код обязан экранировать; см. W7 _reveal как образец).
