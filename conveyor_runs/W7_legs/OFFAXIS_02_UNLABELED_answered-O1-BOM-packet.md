W7 CONVEYOR — OFF-AXIS SUBMISSION (NOT counted as a W7 leg)
REVIEWER: (unlabeled) / MODEL_FAMILY: (none given)
RECEIVED: 2026-08-03

WHY OFF-AXIS: answered the O1/BOM token-hold fork structure, NOT the W7 confusable axes.
  Its "V1-V6" = Leave-as-is / Aggressive token->hold (~67% FP) / Narrow predicate BOM-between-
  letters / Priority-bump-in-queue / Defer-to-guard-canonicalization(вне O1) / Ensemble-score.
  These are the o1-scope-round BOM-invisible variants. W7's real axes are mechanism / FP-boundary /
  target-list / level / batteries / остаток. Model read the W7 header ("mixed-script confusables")
  but ran the wrong packet's variant list. No REVIEWER/MODEL_FAMILY => cannot attribute a family.
  DECISION: does NOT count toward the >=3-family requirement. Not raw material for the W7 svod
  except where it independently echoes Grok (below).

TRANSFERABLE (only because it echoes LEG_01 Grok, so not noise):
  * "do not jump straight to hold -- alarm fatigue" -> first-cut = queue, not hold (== Grok V4).
  * "target-list is an honest остаток; 4 disputed mappings = PENDING, corpus-curated only"
    (== Grok V3/V6).
  * "corpus-driven thresholds / A-B (FP_hold <= 10%)" -> restates the §1 НЕИЗМЕРИМО остаток
    (attack vs legit-mixed frequency needs a corpus). Useful as an acceptance-metric idea IF a
    corpus is ever built; not a first-cut blocker.
  * "defer decision to a canonicalization/position_map layer" -> a mechanism option, but it is the
    GUARD-layer path, not confusable-specific; note for V1 but it does not answer host-gate/IDN/
    fullwidth-dot questions.

NOT ADDRESSED (the actual W7 questions): mixed-script predicate, host-label gate, single-script
  whole-label IDN pass, fullwidth/NFKC dots, skeleton table pin. => re-run this model on the real
  W7 packet if its family is wanted for the >=3 count.
