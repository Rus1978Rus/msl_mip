ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: BIDI_SVOD_AND_SIM_2026-08-04
DOCUMENT_TYPE: COORDINATOR_SVOD + ADVERSARIAL_SIM
IN_RESPONSE_TO: CONVEYOR_PACKET_BIDI_2026-08-04
DATE: 2026-08-04
PROJECT: MSL/MIP
STATUS: DELIVERED_TO_AUTHOR — PENDING_AUTHOR_DECISION
RULE: design-adversarial-sim (свод НЕОБХОДИМ, но НЕ достаточен; замер бьёт голос)

============================================================
§0. СОСТАВ КРУГА
============================================================
ВАЛИДНЫЕ СЕМЕЙСТВА (3, разные платформы — требование выполнено):
  LEG_01 Grok / xAI · LEG_02 Gemini / Google · LEG_03 GPT-5.6 / OpenAI (сверка UAX#9 rev.50, UTS#55,
  Trojan Source arXiv 2111.00169)
МЕТОДОЛОГИЧЕСКИЙ ВХОД (4-й, НЕ семейство): REVIEW_RESULT_BIDI (панель Kimi: SECURITY/FP/VERIFY/
  REDTEAM + RESEARCH, одна платформа — оговорено ею самой). Принесла СОБСТВЕННЫЕ прогоны UAX#9
  [ЗАМЕР-RSEC] и 25 ячеек батареи — самый содержательный вход круга.
§1 пакета никем не оспорен. Ловушку §1 (SOLIDUS ≠ bidi) приняли ВСЕ 4 входа.

============================================================
§1. КОНВЕРГЕНЦИЯ (все 4 входа — считать УСТОЯВШИМСЯ)
============================================================
- НОВАЯ ОСЬ (производная над субстратом класса-138), НЕ достройка D-INV-GEN, НЕ семейство «скрытые
  каналы» (здесь ничего не спрятано). Один общий occurrence-индекс, без второго полного скана.
- МЕТКИ (LRM/RLM/ALM) МОЛЧАТ ВСЕГДА — 4/4. Прямой аналог emoji-флуда W4; измеренный §1 линейный рост
  витнесса (×20 → 20) — цена, которую нельзя платить.
- ИЗОЛЯТЫ МОЛЧАТ — 4/4 (рекомендованная практика Unicode 6.3+).
- «RTL-буква рядом» — НЕ триггер (4/4 у панели; GPT допускает лишь как аннотацию): обходится одной
  видимой RTL-буквой, FP-давление не измерено.
- ОБЛАСТЬ «ИМЯ ФАЙЛА» НЕ ВВОДИТЬ — панель 4/4 (контекста нет, эвристика расширения = семантика = D5,
  переоткрытие C6). ЭТО ПРОТИВОРЕЧИТ Grok/GPT, которые строили сигнал на display-sensitive токене
  (см. развилку F1).
- УРОВЕНЬ: queue-паритет; HOLD НЕ ВВОДИТЬ НИКОГДА на этой оси (4/4); host НЕ поднимать (уже queue от
  D-INV-GEN), один вердикт = max, атрибуция = детерминированный список сработавших осей.
- ПОКАЗ: сырую bidi-строку в отчёт НЕ вставлять (4/4) — отчёт сам стал бы носителем перестановки.
- delta-манифест обязателен, тихий ре-базлайн запрещён; остаток с ярлыком БАЙПАС; формулировка
  «bidi закрыт» ЗАПРЕЩЕНА.
- ПОЛНЫЙ UAX#9 не отменён, но разнесён по фазам (GPT V-OTHER-3 + REDTEAM): фаза 1 уже поднимает
  измеренный спуф; фаза 2 добавляет эталонную проекцию с conformance-пином.

============================================================
§2. ATTACK-SIM: ЧТО Я ПРОВЕРИЛ НЕЗАВИСИМО
============================================================
SIM-1 (РЕШАЮЩИЙ ДЛЯ ЦЕНЫ — F4). В отличие от круга VS (где свойства Script в stdlib НЕ ОКАЗАЛОСЬ),
  здесь `unicodedata.bidirectional()` ЕСТЬ и версия совпадает с нашим пином (UCD 16.0.0). Замер:
  все 12 контролей дают точные классы (LRM->L, RLM->R, ALM->AL, LRE/RLE/PDF/LRO/RLO/LRI/RLI/FSI/PDI
  -> одноимённые), strong-RTL буквы тоже (иврит->R, арабский->AL), цифры->EN, точка->CS.
  ВЫВОД: классификация семейств контролей И предикат «RTL рядом» — БЕСПЛАТНЫ, НОЛЬ новых файлов.
  Это УТОЧНЯЕТ позицию панели («реюз пина, 6-й файл манифеста»): для ФАЗЫ 1 не нужен даже 6-й файл.
  DerivedBidiClass.txt понадобится только если понадобится сверка вне stdlib.
SIM-2 (ЦЕНА ФАЗЫ 2 — ПОПРАВКА К ПАНЕЛИ ЗАМЕРОМ). Скачал артефакты 16.0.0:
  BidiBrackets.txt 8 891 Б (193 строки) · DerivedBidiClass.txt 171 034 Б (2 579) ·
  BidiCharacterTest.txt 6 880 649 Б, данных строк **91 707** — число панели ТОЧНОЕ ·
  BidiTest.txt 7 959 988 Б, всего 497 591 строк, данных **493 502** — панель заявила **180 941**,
  РАСХОЖДЕНИЕ ~2.7x. Реальная цена фазы 2 (conformance) ВЫШЕ заявленной: ~14.8 МБ тест-данных и
  ~585 тыс. тест-кейсов. Не блокер, но при r>g надо считать честно.
SIM-3 (ЗАКРЫТ БЛОКЕР ПАНЕЛИ B6 — ценз живого поведения ячеек манифеста). Прогнал 12 ключевых ячеек:
  B1 invoice<RLO>gpj.exe -> pass/witness 1 · B3 СБАЛАНСИРОВАННЫЙ RLO..PDF спуф -> pass/witness 2 ·
  B4 несбалансированный -> pass/1 · пустая область a<RLO><PDF>b -> pass/2 · RLO вокруг 1 знака ->
  pass/2 · abc<RLE>xyz<PDF>def -> pass/2 · сбалансированные изоляты -> pass/2 ·
  ТРИ FP-СТРАЖА панели (cost<RLI>иврит<PDI>-123 · иврит 123<RLM>-456 · email<RLM> иврит) -> ВСЕ pass ·
  иврит+RLM -> pass/1 · host goog<RLO>le.com -> queue (D-INV-GEN).
  => Все FP-стражи СЕГОДНЯ уже pass: их роль — стеречь РЕГРЕССИЮ, а не менять поведение. Целевые
  дельты манифеста: B1, B3, B4 (pass -> queue) + атрибуция host-ячейки.
SIM-4 (ФАКТ-ЧЕК ПАНЕЛИ). Утверждение «слова deprecated для LRO/RLO/LRE/RLE в UCD НЕТ» —
  ПОДТВЕРЖДЕНО: PropList.txt 16.0.0 помечает Deprecated только U+206A..206F (6 знаков), диапазон
  U+202A..202E там отсутствует. Значит «override почти не нужен» (Grok/Gemini) — это РЕКОМЕНДАЦИЯ
  (tr9 §2 «encouraged»), а НЕ нормативный статус; эскалация по присутствию не имеет опоры в стандарте.
SIM-5 (ЧЕСТНАЯ ГРАНИЦА МОЕЙ ПРОВЕРКИ). Конформантной реализации UAX#9 в среде НЕТ (python-bidi,
  unicodedata2, PyICU — не установлены). Поэтому реордер-замеры панели [ЗАМЕР-RSEC] (сбалансированный
  и несбалансированный спуф переставляют идентично; изоляты-защитники дают changed=True; RLO вокруг
  1 знака changed=False; embeddings не переставляют латиницу) я НЕ воспроизвёл независимо —
  они согласуются с UAX#9 X9/X10/BD9 по построению, но помечаю их как НЕПРОВЕРЕННЫЕ МНОЙ.
  Панель в прошлом круге (VS) дала числа, совпавшие с моими ДО ЕДИНИЦЫ, что повышает доверие;
  но одно её число здесь (BidiTest) оказалось неверным — значит проверять надо всё, что дёшево.
  ТРЕБОВАНИЕ К КОДУ: реализация обязана пройти официальные BidiTest/BidiCharacterTest (ячейка 17
  панели) — тогда эти утверждения станут проверяемыми у нас.

============================================================
§3. РАЗВИЛКИ АВТОРУ (после замеров)
============================================================
F1 ЧТО ОТКРЫВАЕТ СИГНАЛ:
  (a) ПАНЕЛЬ: C-клапан (только override/embedding) + B-детектор (факт перестановки по UAX#9) +
      C-модификатор уровня; область НЕ вводить.
  (b) Grok/GPT: override + display-sensitive токен (имя файла/путь) как несущий предикат.
  ЗАМЕР НА СТОРОНЕ ПАНЕЛИ по существу: контекста «имя файла» в ядре НЕТ (§1: FREE_TEXT на offset RLO),
  эвристика «точка+суффикс» = семантика (D5) и переоткрывает C6. НО (b) дешевле и не требует UAX#9.
  РЕКОМЕНДАЦИЯ: фазность (см. F3) — фаза 1 без области и без UAX#9, по классу+области-действия.
F2 БАЛАНС КАК ТРИГГЕР: Gemini — несущий (стек) | GPT — «улика, не решение» | ПАНЕЛЬ — понижен до
  witness-аннотации 3:1 (диссент VERIFY записан), т.к. сбалансированный спуф переставляет идентично.
  РЕКОМЕНДАЦИЯ: **баланс = аннотация, не триггер** (совпадает у GPT и панели; замер SIM-3 показывает,
  что B3-ячейка сегодня pass — значит именно она и есть цель фронта).
F3 ФАЗНОСТЬ (снимает спор о UAX#9):
  ФАЗА 1 (БЕЗ UAX#9, стоит НОЛЬ новых артефактов — SIM-1): клапан по классу контроля (override/
  embedding открывают; метки/изоляты — никогда) + область действия >1 знака + свёрнутый witness
  меток + causal-ledger. Поднимает B1/B3/B4 из pass в queue.
  ФАЗА 2: полный UAX#9 + пин BidiBrackets/DerivedBidiClass + conformance (BidiTest 493 502 +
  BidiCharacterTest 91 707) + эталонная проекция и показ разницы.
  РЕКОМЕНДАЦИЯ: **делать фазу 1 сейчас, фазу 2 отдельным инкрементом** (GPT V-OTHER-3 + REDTEAM;
  панель поставила это вариантом 2 своего ранга).
F4 ПОКАЗ РАЗНИЦЫ: Gemini — запрет навсегда | Grok — запрет в first-cut | GPT и панель — обе строки,
  ОБЕ обезврежены, байты отчёта содержат НОЛЬ из 12 контролей (байт-скан как ячейка).
  РЕКОМЕНДАЦИЯ: в фазе 1 — позиции+класс+длина области (разница не доставляется, честно сказать);
  в фазе 2 — двойной показ по контракту (данные+индексы, инертные ASCII-метки, оглавление
  «эталон UAX#9, не ваш рендерер»).
F5 ФОРМА: новая ось (3 леги + панель 3:1) vs достройка D-INV-GEN (диссент REDTEAM: дешевле).
  РЕКОМЕНДАЦИЯ: **новая ось** с правилом композиции (один вердикт = max, атрибуция = список осей).

============================================================
§4. БЛОКЕРЫ ДО КОДА
============================================================
B1 Выбор F1–F5 автором.
B2 ПИН ОПРЕДЕЛЕНИЙ: «область действия» контроля (до PDF / до конца параграфа по X-семантике,
  сырое гнездование независимо от кэпа 125) и — для фазы 2 — «перестановка» (один прогон + две
  проекции + сравнение глиф-порядка ПОСЛЕ удаления Cf; наивное сравнение display-строк даёт ложное
  срабатывание — заявление панели, у нас не воспроизведено, обязано покрываться ячейкой).
B3 КОНТРАКТ ПОКАЗА (аннекс к D6/B3 TAG/VS): байт-скан отчёта = 0 из 12 контролей; ASCII-нейтрализация
  (стрип контролов НЕДОСТАТОЧЕН — оставшиеся RTL-буквы переставит терминал читателя); оглавление
  «эталон, не ваш рендерер».
B4 CAUSAL-LEDGER (механизация ловушки §1, GPT+панель): в отчёте атрибуция вклада осей; для
  Trojan-Source-строки — С RLO находка bidi обязана существовать НЕЗАВИСИМО, БЕЗ RLO — исчезнуть.
  Мутация «засчитать SOLIDUS-queue как успех bidi» обязана ломать тест.
B5 DELTA-МАНИФЕСТ по всем полям (eff/attn/witness/триггер-лист), fail-visible census; целевые дельты
  из SIM-3: B1, B3, B4 (pass→queue) + атрибуция host-ячейки; CONTROL: три FP-стража, изоляты,
  метки ×1/×5/×10/×20, пустая область, RLO вокруг 1 знака, RLE-строка.
B6 ЗАКРЫТ ЗАМЕРОМ (SIM-3).
B7 Если берётся фаза 2 — строки манифеста на BidiBrackets/DerivedBidiClass + conformance-гейт
  (реальные размеры из SIM-2, не заявленные).
B8 РЕЕСТР ОСТАТКА (9 маркеров панели) + запрет «bidi закрыт».

============================================================
§5. RANKED_OUTCOME + ВЕРДИКТ
============================================================
1. ФАЗА 1 СЕЙЧАС: клапан по классу (override/embedding) + область действия >1 знака -> queue;
   метки и изоляты молчат конструктивно; свёрнутый witness меток; causal-ledger; queue-паритет;
   host не поднимать; delta-манифест. ЦЕНА: НОЛЬ новых артефактов (SIM-1), O(n).
   ЗАКРЫВАЕТ: B1 + B3 (сбалансированный!) + B4 из измеренного pass.
2. ФАЗА 2 ОТДЕЛЬНО: полный UAX#9 + conformance-пин + эталонная проекция + двойной показ.
   Даёт то, чего фаза 1 не даёт: ФАКТ перестановки и доставку разницы человеку.
3. ОТКЛОНЕНО: «любой bidi -> queue» (измеренный RTL-флуд), «баланс как триггер» (сбалансированный
   спуф идентичен), «RTL рядом» как триггер (обход одной буквой), область «имя файла» (нет контекста,
   D5/C6), самописный strip-and-compare вместо конформантного UAX#9.
4. HOLD — не вводить на этой оси ни в какой фазе (4/4 + вся дисциплина проекта).

ВЕРДИКТ: ДИЗАЙН ВЕРЕН С ПОПРАВКАМИ. LOOPHOLE не объявлен никем (4/4). Корень C6 снимается
  ПЕРЕФОРМУЛИРОВКОЙ СИГНАЛА: «контроли меняют порядок отображения относительно их отсутствия по
  ЗАПИНЕННОМУ UAX#9» — объективный факт строки относительно стандарта; расхождения конкретных
  рендереров — именованный остаток (BYPASS_CONSUMER_RENDERER).

FORK_STATUS: PENDING_AUTHOR_DECISION (F1-F5 + B1-B8). Код заблокирован.

END
