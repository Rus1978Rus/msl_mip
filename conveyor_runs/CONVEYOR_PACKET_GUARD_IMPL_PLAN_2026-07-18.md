ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: CONVEYOR_PACKET_GUARD_IMPL_PLAN_2026-07-18
DOCUMENT_TYPE: CONVEYOR_PACKET
PACKET_TYPE: REVIEW / PACKET_SUBTYPE: IMPLEMENTATION_PLAN (судится ПЛАН кода, не код)
DATE: 2026-07-18
PROJECT: MSL/MIP
AUTHOR: Руслан Малявский (AUTHOR_DECISION_STATUS_AUTHORITY)

РАМКА ПРОЕКТА: система ОПОВЕЩЕНИЯ, не антивирус. Машина свидетель, не судья.
  Судим ПЛАН реализации гарда ДО кода — потому что гард задевает верифицированный
  ZWSP (21/21). Код пишется ТОЛЬКО после author decision по плану.

============================================================
§1. ФАКТ (не на ревью — данные в теле)
============================================================
ЧТО УЖЕ УТВЕРЖДЕНО (не пересматривать):
- ДИЗАЙН-СКЕЛЕТ гарда APPROVED (AUTHOR_DECISION_20260718_D-GUARD-DESIGN, b12a76d):
  аддитивный, КОНТЕКСТ-СВОБОДНЫЙ (detect→annotate→derive); два артефакта (оригинал
  бит-в-бит + canonical_view + биективная карта позиций + богатые метаданные); НИКОГДА
  не strip вслепую; риск-мэппинг ОТДЕЛЬНЫМ слоем; детект СТРОГО до нормализации;
  поглощает регистратор; охват-138-сразу + policy поэтапно; не-контаминация ZWSP =
  аддитивность + ОБЯЗАТЕЛЬНЫЙ acceptance-гейт (НЕ bypass).
- ZWSP ВЕРИФИЦИРОВАН: батарея 21/21 (tests/sim_bycode_v2.py) + mutation-adequacy 5/5 +
  oracle (tests/zwsp_oracle_manifest.py). Гард обязан не сломать это.

ТОЧКА ИНТЕГРАЦИИ (ПРОЧИТАНА ИЗ РЕПО — msl_mip_runtime.py, факт, не гипотеза):
- РЕГИСТРАТОР УЖЕ ПРОТО-ГАРД: `_invisible_candidate()` (STAGE_A) детектит класс по
  СВОЙСТВАМ (Cf / Default_Ignorable из pinned UCD / bidi / join 200C-200D / variation /
  braille) — это и есть класс-138 (CONTROL) + whitespace-семейство отдельно.
- Для CONTROL-СЕМЕЙСТВА (= весь класс-138) STAGE_B витнесит ВЕЗДЕ, КОНТЕКСТ-НЕЗАВИСИМО
  (комментарий кода: «CONTROL family -> witness EVERYWHERE»). Контекст-гейтинг
  (`_reconstructed_context`, removal-probe «strip чтобы УВИДЕТЬ») применяется ТОЛЬКО к
  WHITESPACE-семейству, которого в 138 НЕТ. => для 138 регистратор УЖЕ контекст-свободен.
- `scan_uncarded_invisibles()` исключает КАРТОЧНЫЕ знаки (`if ord(ch) in carded`).
- `_witness_record()` уже TRIVALENT/UNVERIFIABLE, вердикт НЕ меняет, НЕ удаляет
  (Default_Ignorable != safe-to-delete) — ровно рамка гарда.
- `analyze()` держит `uncarded_invisibles` ОТДЕЛЬНЫМ полем отчёта (не в semantic/
  effective_action) — готовый аддитивный слот для shadow-выхода гарда.
- `_demask()` + F-NEW-1 (sequence_engine): «strip чтобы УВИДЕТЬ домен, не решать» —
  урок D5-бага уже в коде (второй невидимый пережил demask → молча пропущенный хост).
СЛЕДСТВИЕ: гард для 138 = в основном ФОРМАЛИЗАЦИЯ регистратора + аддитивные метаданные,
  НЕ greenfield и НЕ деструктивное переписывание.

============================================================
§2. ВОПРОС РЕВЬЮЕРАМ (двигатель — план, «где сломается»)
============================================================
Верен ли предложенный ПЛАН реализации (§3)? Конкретно:
1. Гарантирует ли первый инкремент + acceptance-гейт, что ZWSP 21/21 + mutation 5/5 +
   oracle ОСТАНУТСЯ зелёными? Докажите механизмом, не «должно пройти».
2. Верен ли план утверждённому скелету (аддитивный/контекст-свободный/два артефакта/
   no-blind-strip/риск-отдельно) или ДРЕЙФует?
3. Не создаёт ли сам план новую поверхность атаки (порядок, потеря позиции, desync)?
Дайте ответ по критериям §4 + «где дыра». Открытая графа — своё измерение/LOOPHOLE.

============================================================
§3. ПРЕДЛАГАЕМЫЙ ПЛАН (на ревью — это ГИПОТЕЗА, не решение)
============================================================
ИНКРЕМЕНТ 1 — SHADOW-АННОТАТОР (аддитивный, вердикт не трогает):
  - Новая ЧИСТАЯ функция `class_guard_annotate(text) -> GuardResult`:
      original       : text бит-в-бит (не копия-мутант);
      members[]      : на каждый член класса-138 — {codepoint, offset, class_bucket
                       (PURE/DIRECTIONAL/TAG/DEPRECATED), family, card_mask (есть ли
                       карточка)};
      canonical_view : производный вид (пока НЕ используется в вердикте — только строится);
      position_map   : биективная orig<->canon (тотально конструируема, Kimi P7);
      schema_version : версия контракта метаданных (Qwen).
  - Покрывает ВСЕ 138 (карточные + бескарточные) — в отличие от регистратора (тот
    карточные исключает). Детект по СВОЙСТВУ (переиспользует `_invisible_candidate`),
    КОНТЕКСТ-СВОБОДЕН для CONTROL/138.
  - Встраивается в `analyze()` как НОВОЕ поле отчёта (`class_guard`), рядом с
    `uncarded_invisibles`. НЕ входит в single_actions/relation_actions/semantic_action/
    effective_action. Вердикт НЕ меняется — shadow.
  - Порядок: detect/annotate ДО любой нормализации; strip НЕ применяется в вердикт-пути.

ACCEPTANCE-ГЕЙТ (обязателен, D4b 5/5):
  - Прогнать sim_bycode_v2 (21/21) + mutation-adequacy 5/5 + oracle С включённым
    гардом; ASSERT: все кортежи/вердикты/witness/positions ИДЕНТИЧНЫ; единственная
    разрешённая дельта = новое аддитивное поле `class_guard`.
  - Мутации самого гарда (GPT MUT-G1..G7): удалить trace до матчера; неверная
    orig<->canon карта; strip карточного ZWSP; классифицировать ZWJ как safe-to-delete;
    ведущий BOM как обычный внутренний; тихий сброс overflow; смена версии без provenance.
    Батарея гарда должна ловить каждую.
  - БЕЗ зелёного гейта — мержа нет.

ПОЗЖЕ (НЕ в инкременте 1, отдельными заходами):
  - Инкремент 2: регистратор → потребитель гарда (единый сканер, D6); карточные
    получают класс-аннотацию; sequence-поля (SEQUENCE_TYPE/VALIDITY, GPT); resource-
    limits (O(n), event-limit, TRACE_TRUNCATED, GPT).
  - Policy-виды (strip) и рантайм-ремэппинг (O1) — вне этого плана.

============================================================
§4. КРИТЕРИИ ОЦЕНКИ ПЛАНА
============================================================
(а) ДОКАЗЫВАЕТ ли не-контаминацию ZWSP — механизм, гарантирующий 21/21 зелёными
    (аддитивность = вердикт-путь не трогается; гейт = ре-прогон assert идентичности).
(б) ВЕРЕН ли утверждённому скелету — аддитивный/контекст-свободный/два артефакта/
    no-blind-strip/риск-отдельно/детект-до-нормализации. Дрейф?
(в) МИНИМАЛЕН и БЕЗОПАСЕН ли первый инкремент — не меняет вердикт, только новое поле?
(г) ПЕРЕИСПОЛЬЗУЕТ ли существующее (регистратор/_demask/candidate) вместо дублирования
    (r>g — не растит g параллельным сканером)?
(д) ЛОВИТ ли acceptance-гейт регрессии — достаточен ли набор (батарея+mutation+oracle+
    guard-мутации), нет ли дыры, через которую поломка ZWSP пройдёт незамеченной?
Ревьюер: 5 отметок + BASIS; ЯВНО — гарантирует ли инкремент-1+гейт 21/21; RANKED
(что в плане выживает / что CUT); RECOMMENDATION по первому инкременту.

============================================================
§5. ГРАНИЦЫ
============================================================
- Судим ПЛАН. КОД пишется ТОЛЬКО после author decision по плану.
- ZWSP НЕ ТРОГАЕМ до зелёного acceptance-гейта.
- НЕ пересматриваем утверждённый скелет (D-GUARD-DESIGN), класс 138, HYBRID_C.
- Policy-strip и рантайм-ремэппинг (O1) — вне этого круга.

============================================================
§6. ДИСЦИПЛИНА (приложена)
============================================================
- foundation_layer/AUTHOR_DECISION_20260718_D-GUARD-DESIGN.md — УТВЕРЖДЁННЫЙ скелет (BASIS).
- foundation_layer/ARCH_DECISION_INVISIBLE_SIGNS_HYBRID_C.md — архитектура.
- foundation_layer/CLASS_FRONT_INVISIBLE_SIGNS.md — ре-валидация карточек класса.
- tests/sim_bycode_v2.py + tests/zwsp_oracle_manifest.py — батарея 21/21 + oracle (гейт).
- msl_mip_runtime.py (_invisible_candidate/scan_uncarded_invisibles/analyze),
  sequence_engine.py (_demask/F-NEW-1) — точки интеграции (в теле §1).
- FOUNDATION_CONCEPT_PIKETTY_R_G — r>g (переиспользовать, не дублировать).
- RULE_WHEN_CONVEYOR_REQUIRED_v0_1 — почему план на конвейере (задевает верифицированное).

============================================================
DELIVERABLE_FORMAT
============================================================
REVIEW_RESULT: REVIEWER / MODEL_FAMILY (семейств >=3); по каждому критерию (а-д) —
  отметка + BASIS; ЯВНЫЙ ответ: гарантирует ли инкремент-1 + acceptance-гейт ZWSP
  21/21; RANKED (план выживает / дрейф / CUT); RECOMMENDATION по первому инкременту +
  где дыра гейта; явно — верен ли план скелету или дрейфует.
REQUIRED_REVIEWERS >=3, семейств >=3.
EXIT: план сходится → AUTHOR_DECISION → код (с обязательным зелёным ZWSP-гейтом);
  расходятся → материал для AUTHOR_DECISION; план ломает ZWSP / дрейфует от скелета →
  LOOPHOLE, пере-собрать.

FORK_STATUS: OPEN — PENDING_CONVEYOR + AUTHOR_DECISION (план реализации гарда).
  Код заблокирован до author decision по плану.

END_OF_CONVEYOR_PACKET
