R4 CONVEYOR LEG 04
REVIEWER: GPT-5.6 Thinking / MODEL_FAMILY: OpenAI GPT (ЧЕТВЁРТОЕ семейство)
RECEIVED: 2026-08-06
PACKET: CONVEYOR_PACKET_R4_SLOPPY_VS_PAYLOAD_2026-08-06
NOTE: сверка UTS#39 (join-control контексты — минимальные, не полный языковой оракул).

VERDICT: ОСТАТОК ПОДТВЕРЖДЁН КАК ФУНДАМЕНТАЛЬНЫЙ. LOOPHOLE НЕТ, но FUNDAMENTAL_INFORMATION_LIMIT — ДА.
  Двухносительное правило СОХРАНИТЬ; универсальный однознаковый вердикт НЕ вводить;
  ЧАСТИЧНОЕ покрытие в машинных контекстах ПРИНЯТЬ; родная письменность — только свёрнутый витнесс.

ФОРМАЛИЗАЦИЯ ПРЕДЕЛА (сильнее всех предыдущих лег):
  наблюдаемый вход — последовательность S; история A — человек поставил лишние ZWNJ; история B —
  атакующий использовал наличие ZWNJ как бит. На входе анализатора строки ИДЕНТИЧНЫ. Значит ЛЮБАЯ
  детерминированная f(S) → вердикт ОБЯЗАНА вернуть одинаковый ответ для обеих историй.
  ⇒ это НАБЛЮДАТЕЛЬНАЯ НЕРАЗЛИЧИМОСТЬ, а не нехватка эвристики. Разрешается ТОЛЬКО добавлением
  ВНЕШНИХ данных: доверенный эталон · история правок · происхождение ввода · словарь/морфология ·
  схема поля потребителя · корпус · межсообщенческое поведение.
  «Это должно быть записано в author decision как ПРЕДЕЛ ИНФОРМАЦИИ, а не как backlog обычного matcher.»

POSITIONS:
  V1 -> E, но РАЗБИТОЕ ПО КОНТЕКСТУ (никто другой этого не предложил):
     ветка 1 СИЛЬНАЯ СТРУКТУРНАЯ НЕВОЗМОЖНОСТЬ → возможен QUEUE, если все/почти все вхождения
       NONFUNCTIONAL И контекст НЕ относится к письменности, где носитель имеет функцию, И сегмент
       имеет доверенный профиль (машинное поле / идентификатор / токен / код / ключ конфига),
       И достигнут авторизованный порог улик;
     ветка 2 РОДНАЯ ПИСЬМЕННОСТЬ → SINGLE_CARRIER_NATIVE_AMBIGUITY → свёрнутый витнесс, БЕЗ вердикта;
     регулярность позиций и соотношения → ТОЛЬКО телеметрия;
     полное универсальное покрытие → ЯВНО НЕРЕШЕНО.
     Про регулярность: подтверждает «четвёртый управляемый параметр» и добавляет контрдовод —
     редактор/шаблонный импорт/систематическая ошибка клавиатуры дают регулярность БЕЗ атаки.
  V2 -> (i) принять + УПРАВЛЯТЬ. Ключевая формула: **EQUAL RULE TEXT != EQUAL IMPACT** —
     одинаковое правило для всех письменностей было бы формально одинаковым, но фактически
     несправедливым. Требует именованный SCRIPT_COST_ASYMMETRY_REGISTER (затронутые письменности,
     источник асимметрии, защищённое поведение, остаток, пробел в доказательствах).
  V3 -> порог 16 = ВЕРСИОНИРУЕМАЯ КОНСТАНТА ПОЛИТИКИ; произвольная числовая настройка ЗАПРЕЩЕНА
     (потребитель поставит 1000000 и выключит фронт, либо 1 и получит тревогу на каждом ZWNJ);
     допустимы ДОВЕРЕННЫЕ ПРОФИЛИ после author decision: GENERAL_TEXT / PROTECTED_MACHINE_FIELD /
     NATIVE_SCRIPT_EDITOR / STRICT_FORENSIC. Профиль задаётся доверенным слоем, фиксируется в отчёте,
     НЕ выбирается содержимым, не меняет определения FUNCTION_STATUS, имеет версию и delta-census.
     ⇒ ЭТО СИНТЕЗ развилки F1: не «крутилка» (Qwen) и не «ничего» (Grok/Gemini), а профили.
  V4 -> ***РАЗВЯЗКА РАЗВИЛКИ F2 ДВУМЯ МАСШТАБАМИ:*** VERDICT_SEGMENT = слово/токен (как сейчас),
     OBSERVATION_SCOPE = абзац/поле. Вердиктная логика по словам, свёрнутая сводка по абзацу.
     «Позволяет увидеть 25 рассыпанных ошибок ОДНОЙ строкой, не превращая их в carrier verdict».
     Объект SINGLE_CARRIER_RESIDUAL_SUMMARY (носитель, охват, счётчики по четырём статусам, verdict
     NOT_APPLIED, причина SINGLE_CARRIER_ORIGIN_UNRESOLVED, известный остаток, выборка позиций,
     число опущенных). Порог показа: один тип + >=16 в охвате + >=1 NONFUNCTIONAL/UNVERIFIABLE.
     ПРЯМО ОТВЕЧАЕТ на возражение Gemini: было 240 строк — стало 1 структурированная запись.
     Про раскрытие: **ABSENCE IS NOT AN OBSERVED CODEPOINT** — для presence/absence схемы битовый
     поток показывать НЕЛЬЗЯ (неизвестны множество слотов, значение «отсутствует», порядок, начало).
  V5 -> манифест AUTHORIZED_R4_SINGLE_CARRIER_DELTA_MANIFEST_v0_1; группы A (известная слепота,
     статус KNOWN_SINGLE_CARRIER_BLIND_SPOT, НЕ «CLEAN»), B (неряшливость), C (слитный сегмент),
     D (живая двухносительная ось — НЕЛЬЗЯ ослаблять ради R4), **E (легитимный смешанный копипаст —
     обязательный новый контроль E1/E2/E3)**, F (сегментация 240 бит / 16 и 240 сегментов).
     ЧЕСТНАЯ ОГОВОРКА: «до измерения E1–E3 НЕЛЬЗЯ утверждать, что ложные срабатывания R4 практически
     равны нулю во ВСЕХ copy/paste-сценариях»; возможно, потребуется минимальная ДОЛЯ или ЧИСЛО
     нефункциональных — «но такие условия не вводятся до замера».
  V6 -> LIMIT_ID: **NATIVE_SCRIPT_SINGLE_CARRIER_INDISTINGUISHABILITY**,
     STATUS: FUNDAMENTAL_OBSERVATIONAL_LIMIT. Десять пунктов остатка, включая ошибки самого
     функционального оракула и смешанный копипаст.
  V-OTHER-2 -> ***REFERENCE-AWARE РЕЖИМ (новая идея круга):*** там, где есть доверенный ЭТАЛОН
     (подписанный оригинал, allowlist-значение, две редакции документа, результат OCR/импорта),
     сравнение даёт детерминированную дельту позиций носителей ⇒ точный QUEUE БЕЗ предположений
     о персидской орфографии. Но это ОТДЕЛЬНЫЙ вход с provenance, не общий текстовый режим.
  V-OTHER-3 -> IMPACT-GATE: любая будущая эскалация одиночного носителя в родной письменности
     ЗАБЛОКИРОВАНА до наличия корпуса, per-script FP-rate, корпуса доброкачественного копипаста,
     miss-rate на рандомизированных носителях, авторского FP-бюджета, script impact statement и
     авторизованного delta-манифеста. «NO SCRIPT IMPACT MEASUREMENT → NO NATIVE-SCRIPT QUEUE RULE».

=== ЗАМЕР КООРДИНАТОРА: ПРОВЕРКА ОБОИХ ПРЕДЛОЖЕНИЙ GPT ===
(1) ЧАСТИЧНОЕ ПОКРЫТИЕ В МАШИННОМ/ЛАТИНСКОМ КОНТЕКСТЕ — ИЗМЕРЕНО, СТОИТ НОЛЬ:
    атака 24 ZWNJ в латинском идентификаторе -> 24 нефункциональных, все не-краевые;
    атака 24 ZWNJ в кириллическом токене     -> 24 нефункциональных;
    ЛЕГИТ обычный английский текст            -> **0 носителей**;
    ЛЕГИТ латинский идентификатор             -> **0 носителей**;
    ЛЕГИТ персидский аккуратный               -> 24 носителя, **0 нефункциональных**.
    ⇒ В латинице/кириллице легитимный текст даёт РОВНО НОЛЬ носителей — ZWNJ там просто не пишут.
    Правило «одиночный носитель + все позиции нефункциональны + латинский/машинный контекст → queue»
    стоит НОЛЬ ложных срабатываний. **Покрытие бесплатно.** Трое коллег отказались ловить
    однознаковую схему ВООБЩЕ и тем самым отдали даром то, что можно взять без цены.
    В персидском то же правило неотличимо от опечаток ⇒ остаётся витнессом. GPT прав по обеим веткам.
(2) ДВА МАСШТАБА — ИЗМЕРЕНО, РАЗВИЛКА F2 СНИМАЕТСЯ:
    документ 30 слов-сегментов, 30 носителей, 25 нефункциональных;
    вердикт по СЛОВУ: каждое слово 1 носитель -> ниже порога -> тишина (поведение не меняется);
    сводка по АБЗАЦУ: одна строка «25 нефункциональных одного типа» -> человек видит.
    ⇒ возражение Gemini (шум) и требование Grok/Qwen (видимость) выполняются ОДНОВРЕМЕННО,
    потому что это РАЗНЫЕ МАСШТАБЫ, а не разные ответы на один вопрос.

СЧЁТ ПО РАЗВИЛКАМ (4 семейства): F1 порог — «никаких крутилок» Grok+Gemini (2) | «контекст у
  интегратора» Qwen (1) | **профили с версией и delta-census GPT (1) = СИНТЕЗ обоих**.
  F2 аннотация — за Grok+Qwen (2) | против Gemini (1) | **два масштаба GPT (1) = СИНТЕЗ**.
  F3 НОВАЯ развилка: частичное покрытие машинных контекстов — только GPT (1) против «не ловить
  вообще» (3), НО замер на стороне GPT: покрытие бесплатно.
