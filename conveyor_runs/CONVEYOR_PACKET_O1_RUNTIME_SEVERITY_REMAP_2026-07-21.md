ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: CONVEYOR_PACKET_O1_RUNTIME_SEVERITY_REMAP_2026-07-21
DOCUMENT_TYPE: CONVEYOR_PACKET
PACKET_TYPE: REVIEW / PACKET_SUBTYPE: ARCHITECTURE_DESIGN (судится ДИЗАЙН O1 — рантайм-
  риск-ремэппинга context→severity)
DATE: 2026-07-21
PROJECT: MSL/MIP
AUTHOR: Руслан Малявский (AUTHOR_DECISION_STATUS_AUTHORITY)

РАМКА ПРОЕКТА: система ОПОВЕЩЕНИЯ, не антивирус. Машина свидетель, не судья. Уровень
  тревоги — РЕКОМЕНДАЦИЯ человеку, не приговор. Вердикт ревьюера — рекомендация; финальный
  статус присваивает автор. Судим ДИЗАЙН O1 ДО кода — потому что O1 задевает
  верифицированный ZWSP (батарея 21/21), нельзя наскоком.

============================================================
§1. ФАКТ (не на ревью — данные в теле)
============================================================
ЧТО РЕШЕНО (не пересматривать):
- ТРЁХУРОВНЕВЫЙ ПРИНЦИП (автор 2026-07-18): тревога = ЧИСТО / ВОЗМОЖНАЯ ОПАСНОСТЬ /
  РЕАЛЬНАЯ ОПАСНОСТЬ. Как КОНЦЕПТ — не пересматривается.
- КЛАСС = поднадзорный класс 138 (Cf∧Default_Ignorable; D-NEIGHBORS-BORDER-138).
- ГАРД: дизайн-скелет УТВЕРЖДЁН (D-GUARD-DESIGN APPROVED). КЛЮЧЕВОЕ для O1 — измерение D3
  (5/5): РИСК-МЭППИНГ = ОТДЕЛЬНЫЙ слой НАД гардом; гард несёт presence-сигнал класса, но
  НЕ risk-вердикт. То есть архитектура УЖЕ зарезервировала место под O1 как отдельный слой.
- WITNESS-РАМКА: РЕАЛЬНАЯ ОПАСНОСТЬ = громкая РЕКОМЕНДАЦИЯ человеку (приоритет), НЕ
  авто-блок. O1 меняет УРОВЕНЬ сигнала, не превращает машину в судью.

ТЕКУЩАЯ КАРТА РАНТАЙМА (субстрат, который O1 ремэпит — из исходника, VERIFIED прогоном):
  sequence_engine.py:653 `_SCOPE_RISK` (context → RiskLevel):
    HOST = HIGH · EMAIL = MEDIUM · BYTE_EXACT_TOKEN = MEDIUM · URL/PATH/QUERY_VALUE/
    FRAGMENT/USERINFO = MEDIUM · HIDDEN_BOUNDARY_PADDING = MEDIUM · FREE_TEXT = NONE.
  sequence_engine.py:741 ГЕЙТ срабатывания ребра:
    `if role=="PRIMARY" and protected and ctx != "FREE_TEXT": risk=_SCOPE_RISK.get(ctx)`
    `else: risk = NONE`  (FREE_TEXT исключён БЕЗУСЛОВНО, для ВСЕХ знаков).
  sequence_integrator_engine.py:29 RiskLevel → action:
    NONE→pass · LOW→log_only · MEDIUM→queue_for_review · HIGH→hold_pending_review ·
    CRITICAL→escalate_to_human.
  ТРЁХУРОВНЕВЫЙ МАППИНГ (карточка BOM 7A): pass=ЧИСТО · queue=ВОЗМОЖНАЯ · hold=РЕАЛЬНАЯ.
  ВАЖНО: карта — ПО КОНТЕКСТУ, ОДНА на все невидимые. Знак в неё НЕ входит.

ЧТО O1 ДОЛЖЕН РАЗРЕШИТЬ (входы из RECONCILE_BYSPEC_ZWJ_BOM_2026-07-18):
- BOM THREE_LEVEL_DIVERGENCE — 4 расхождения ЖЕЛАЕМО РЕАЛЬНАЯ / ИЗМЕРЕНО ВОЗМОЖНАЯ:
  токен, идентификатор, email, ведущий-перед-доменом (все сейчас MEDIUM→queue).
- Z5 (free-text ZWJ) — ЖЕЛАЕМО ВОЗМОЖНАЯ / ИЗМЕРЕНО ЧИСТО (слепые ноги строже кода;
  известный RISK_CASE_004 free-text padding PENDING). Сейчас FREE_TEXT→NONE→pass.
- Z4/B3 (ZWJ/BOM токен-email) — «строгие» слепые ноги дали РЕАЛЬНАЯ; код на MEDIUM.

КРИТИЧЕСКИЙ ФАКТ (почему O1 судится ДО кода — ядро напряжения):
- Желаемые ремэпы — ПО ПАРЕ (ЗНАК × КОНТЕКСТ), а карта сегодня — ПО КОНТЕКСТУ ОДНА.
  Доказательства расхождения знаков в ОДНОМ контексте:
    · ведущий-домен: BOM → хотим РЕАЛЬНАЯ; ЗWSP в том же контексте — верифицирован как
      ВОЗМОЖНАЯ (queue), поднимать нельзя.
    · free-text: ZWJ → хотим ВОЗМОЖНАЯ; ZWSP free-text — верифицированный ЧИСТО (pass),
      это контрольный кейс N1 батареи, поднимать = сломать 21/21.
- Значит НАИВНАЯ правка общей `_SCOPE_RISK` или гейта стр.741 (снять `ctx!="FREE_TEXT"`)
  КОНТАМИНИРУЕТ верифицированный ZWSP. Оба замка — общий путь, по нему течёт ZWSP и
  mask/relation-ось. O1 обязан эскалировать ИМЕННО BOM/ZWJ в ИМЕННО этих контекстах,
  НЕ трогая ZWSP.

ДИЗАЙН-СВИДЕТЕЛЬСТВО (внешнее, НАЛОЖЕНО и прочитано построчно 2026-07-20/21, НЕ мерж):
  INVARIANT_ENGINE_SUITE / code/range/invisible_cards.py — независимо реализует
  ОТДЕЛЬНЫЙ per-sign контекстный слой серьёзности (ALARM/WATCH/OK) НАД ядром, аддитивно
  (severity-max combine), НЕ правя общую карту. Воспроизведён на нашем ядре (benign
  5/8→8/8, 0 новых FP). Это НЕЗАВИСИМОЕ переизобретение трёхуровневого + границы
  functional-vs-redundant (Z3). Годен как свидетельство в пользу «отдельный слой», НЕ как
  код на мерж (часть его — brand-lookup — воюет со «structure-only», к O1 не относится).
  См. [[invariant-suite-overlay]].

============================================================
§2. ВОПРОС РЕВЬЮЕРАМ (двигатель круга — дизайн, «как обойдут / где сломается»)
============================================================
Спроектировать O1 (рантайм-риск-ремэппинг) так, чтобы он: доставил желаемые
трёхуровневые эскалации (BOM token/email/ident/ведущий-домен → РЕАЛЬНАЯ; ZWJ free-text →
ВОЗМОЖНАЯ; Z4/B3) НЕ трогая общий путь context→severity, от которого зависит
верифицированный ZWSP (21/21); остался в WITNESS-рамке (уровень = рекомендация, не
авто-блок); прошёл r>g (не рос g линейно на каждый знак/контекст); и САМ не создал
поверхность (пере-эскалация = alarm fatigue = провал свидетеля). По КАЖДОМУ измерению §3 —
ответ + «где дыра / чем обходится». Предложи своё измерение в открытой графе.

============================================================
§3. ИЗМЕРЕНИЯ ДИЗАЙНА (на ревью)
============================================================
O1-D1. ГДЕ ЖИВЁТ РЕМЭП (ядро). Кандидаты (не ограничивать):
    D1a. править общую `_SCOPE_RISK` / снять `ctx!="FREE_TEXT"` в гейте стр.741 —
         прямо, но задевает ZWSP (нужен способ не задеть);
    D1b. per-sign слой серьёзности НАД общей картой, АДДИТИВНЫЙ (общая карта неизменна;
         знак-специфичная эскалация добавляется сверху; ZWSP не трогается) — зеркалит
         D3 гарда и внешнее свидетельство invisible_cards;
    D1c. гибрид.
    Какой вариант доставляет эскалации BOM/ZWJ И гарантированно НЕ двигает ZWSP?

O1-D2. ФОРМА ПОЛИТИКИ: серьёзность = f(ЗНАК × КОНТЕКСТ), а не f(КОНТЕКСТ)? Верна ли
    per-(sign,context) таблица политики (BOM/ZWJ расходятся с ZWSP в ОДНИХ контекстах)?
    Где она ломается / где растит g (137 знаков × N контекстов)? Как не переобучить?

O1-D3. ОТНОШЕНИЕ К ГАРДУ. O1 — ЭТО зарезервированный риск-слой гарда (D-GUARD-DESIGN
    D3: «риск отдельным слоем над гардом»), или отдельная сущность? Слить в один слой
    серьёзности класса, или держать раздельно? Что не создаёт двойного источника истины?

O1-D4. Z5 / FREE_TEXT (точечный тест дизайна). Как выразить «ZWJ в free-text →
    ВОЗМОЖНАЯ, а ZWSP в free-text → ЧИСТО (верифицированный N1)» НЕ трогая безусловный
    гейт стр.741 `ctx!="FREE_TEXT"`? Если free-text перестаёт быть плоским исключением —
    чем доказать, что ZWSP-free-text-pass (и вся 21/21) не сдвинулись?

O1-D5. НЕ-КОНТАМИНАЦИЯ (главный риск). Как построить O1, чтобы батарея ZWSP 21/21 +
    mutation-adequacy 5/5 + oracle ГАРАНТИРОВАННО переприйти? Acceptance-гейт против O1
    (не мержить, пока не зелёный). Что СТРУКТУРНО (не «прогоном повезло») гарантирует,
    что ремэп касается только BOM/ZWJ-целевых пар?

O1-D6. WITNESS-РАМКА. РЕАЛЬНАЯ ОПАСНОСТЬ должна маппиться в hold_pending_review
    (громкая рекомендация) — но выше есть escalate_to_human. РЕАЛЬНАЯ = hold или escalate?
    Где O1 рискует соскользнуть из свидетеля в судью (авто-эскалация без человека)? Как
    удержать «последнее слово за человеком»?

O1-D7. ОХВАТ И ЭТАПНОСТЬ. Ремэпить сначала карточные BOM/ZWJ (где расхождения предъявлены)
    или сразу все 138? Что минимально-полезно без переобучения на 10 контекстов reconcile?

O1-OTHER (открытая графа): ревьюер называет измерение вне D1–D7 с ответом по §4. Если
    видит, что рантайм-ремэп как идея неверен (напр. серьёзность вообще не место рантайма,
    а карточки/гарда) — сказать прямо (LOOPHOLE).

============================================================
§4. КРИТЕРИИ ОЦЕНКИ (по каждому измерению)
============================================================
(а) НЕ ЛОМАЕТ ВЕРИФИЦИРОВАННОЕ — ZWSP 21/21 + mutation 5/5 + oracle переприйдут?
    (жёсткое ограничение: контаминация проверенного = провал дизайна.)
(б) ДОСТАВЛЯЕТ ЖЕЛАЕМЫЕ ЭСКАЛАЦИИ — BOM token/email/ident/ведущий-домен→РЕАЛЬНАЯ; ZWJ
    free-text→ВОЗМОЖНАЯ; Z4/B3 — из reconcile, а не «на глаз».
(в) r>g — знак-специфичная политика не растит g неограниченно; один механизм, не N
    частных правок.
(г) WITNESS-РАМКА — уровень = рекомендация человеку, НЕ авто-блок; «последнее слово за
    человеком» цело.
(д) РЕАЛИЗУЕМ — детерминированно, тестируемо, герметично; acceptance-гейт строится.
(е) НЕ СОЗДАЁТ НОВОЙ ПОВЕРХНОСТИ — пере-эскалация (alarm fatigue) и под-эскалация
    (дыра) равно провал; desync/потеря позиции при ремэпе исключены.
Ревьюер: по каждому измерению — 6 отметок + BASIS; затем RANKED (какие варианты D1/D2/D4
выживают по всем, какие CUT и на чём); RECOMMENDATION (дизайн-скелет O1) как рекомендация,
не статус; явно — считаешь ли рантайм-ремэп верным местом или видишь дыру (LOOPHOLE).

============================================================
§5. ГРАНИЦЫ
============================================================
- Судим ДИЗАЙН O1. НЕ пересматриваем: трёхуровневый принцип КАК КОНЦЕПТ, класс 138,
  образцы, дизайн-скелет гарда (APPROVED), witness-рамку.
- КОД НЕ ПИШЕМ до author decision по дизайну O1. Этот круг — только дизайн-скелет.
- ZWSP НЕ ТРОГАЕМ в этом круге (дизайн говорит КАК его ре-валидировать, но не делает).
- НЕ мержим код пакета INVARIANT_ENGINE_SUITE — invisible_cards только СВИДЕТЕЛЬСТВО
  формы, не реализация на вход.
- Закрытие карточек ZWJ/BOM (WORKINGLY_CLOSED) + EN-зеркало — ОТДЕЛЬНО, не здесь.
- B5 (parser-desync/canonical_view) — смежный фронт гарда инкремента-2, не O1; O1 лишь
  говорит, куда по шкале ляжет desync-сигнал, когда B5 его даст.

============================================================
§6. ДИСЦИПЛИНА (приложена)
============================================================
- RECONCILE_BYSPEC_ZWJ_BOM_2026-07-18 — входы O1 (4 BOM-расхождения + Z5 + Z4/B3).
- AUTHOR_DECISION_20260718_D-GUARD-DESIGN — D3 (риск отдельным слоем над гардом);
  O1 = кандидат в этот слой. O1 в решении гарда — явный OPEN.
- cards BOM/ZWJ (git) — THREE_LEVEL_DIVERGENCE (BOM 7A/OQ2), PER_OCCURRENCE_BOUNDARY (ZWJ).
- sequence_engine.py `_SCOPE_RISK` + гейт стр.741; sequence_integrator RiskLevel→action —
  субстрат ремэпа (в теле §1).
- FOUNDATION_CONCEPT_PIKETTY_R_G — фильтр r>g (O1 ДОЛЖЕН поднять r без роста g).
- INVARIANT_ENGINE_SUITE invisible_cards.py — внешнее свидетельство «отдельный per-sign
  слой» (наложено, воспроизведено; НЕ мерж).
- RULE_WHEN_CONVEYOR_REQUIRED — несколько жизнеспособных дизайнов, прогоном не разводятся.

============================================================
DELIVERABLE_FORMAT
============================================================
REVIEW_RESULT: REVIEWER / MODEL_FAMILY (семейств >=3); по КАЖДОМУ измерению O1-D1..D7
  (+OTHER) — отметки (а)(б)(в)(г)(д)(е) + BASIS; для O1-D1 (где живёт ремэп) и O1-D4
  (Z5/free-text) — ЯВНЫЙ выбор варианта + почему не ломает верифицированное;
  RANKED_OUTCOME (какой дизайн-скелет O1 выживает); RECOMMENDATION (скелет) как
  рекомендация, не статус; явно — рантайм-ремэп верное место или LOOPHOLE.
REQUIRED_REVIEWERS >=3, семейств >=3.
EXIT: дизайн сходится → в AUTHOR_DECISION по дизайну O1 → ТОЛЬКО ПОТОМ код + ре-валидация
  ZWSP; расходятся → материал для AUTHOR_DECISION (не усреднять); рантайм-ремэп неверен /
  вопрос плох → LOOPHOLE, пере-собрать.
СЛЕПОТА: это ДИЗАЙН-круг (судится архитектура ремэпа), НЕ BY_SPEC-нога. Измеренная карта
  context→severity в §1 — СУБСТРАТ дизайна (что ремэпим), не «правильный ответ»; давать её
  ревьюеру законно. НЕ подавать сюда измеренные BY_SPEC-предсказания как оракул.

FORK_STATUS: OPEN — PENDING_CONVEYOR + AUTHOR_DECISION (дизайн O1 RUNTIME_SEVERITY_REMAP).
  Код заблокирован до author decision по дизайну.

END_OF_CONVEYOR_PACKET
