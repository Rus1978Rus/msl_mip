ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: W7_SVOD_AND_SIM_2026-08-04
DOCUMENT_TYPE: COORDINATOR_SVOD + ADVERSARIAL_SIM
IN_RESPONSE_TO: CONVEYOR_PACKET_W7_CONFUSABLE_2026-07-26
DATE: 2026-08-04
PROJECT: MSL/MIP
STATUS: DELIVERED_TO_AUTHOR — PENDING_AUTHOR_DECISION
RULE: design-adversarial-sim (свод НЕОБХОДИМ, но НЕ достаточен; замер на живом ядре бьёт голос)

============================================================
§0. СОСТАВ КРУГА (требование ">=3 РАЗНЫХ семейства" ВЫПОЛНЕНО)
============================================================
ВАЛИДНЫЕ СЕМЕЙСТВА (4, разные платформы):
  LEG_01 Grok / xAI
  LEG_03 Gemini / Google
  LEG_04 Qwen / Alibaba
  LEG_05 GPT-5.6 Thinking / OpenAI (web-verified UTS#39/46/UAX#24)
МЕТОДОЛОГИЧЕСКИЙ ВХОД (5-й, не семейство): REVIEW_RESULT_W7 (4 стойки SECURITY/FP/VERIFY/REDTEAM
  на ОДНОЙ платформе — сам оговаривает, что букву требования не закрывает; корроборирует + даёт блокеры).
ОТКЛОНЕНО: OFFAXIS_02 (unlabeled) — ответил на структуру старого O1/BOM-пакета, не W7; не в счёт.
§1 пакета (замеры) никем не оспорен.

============================================================
§1. КОНВЕРГЕНЦИЯ (4 семейства + свод согласны — считать УСТОЯВШИМСЯ)
============================================================
- V1 = C: одна КЛАССОВАЯ confusable-ось (two-tier по ARCH_DECISION_TWO_TIER); ядро-механизм A
  (mixed-script + skeleton) ЖИВЁТ ВНУТРИ карточной оси (не нарушает VIA_CARD_ONLY). B (81 карта)
  как самостоятельный детектор ОТКЛОНЁН единогласно (измеренный alpha-testing FP).
- V2: host-контекст-гейт ОБЯЗАТЕЛЕН + per-LABEL (не per-host) + триггер = "латиница ∧ confusable-знак
  иного скрипта в ОДНОМ label", НЕ голый ">=2 скрипта".
- V3: generic mixed-script-в-host -> queue БЕЗ списка целей. Список -> только эскалатор до hold.
- V4: queue по умолчанию (паритет W4/W5/D-INV-GEN); host-only first-cut.
- V6: whole-script spoof -> ОТДЕЛЬНЫЙ фронт (target-free детекта не существует, UTS#39 §4.1);
  таблица запинена; 81 Vakhter = nursery/test-corpus, не источник истины.

GPT-патчи, усиливающие консенсус (принять как техническую базу):
  P1 триггер = ФАКТИЧЕСКИЙ UTS#39 confusable-маппинг + resolved-script (Script_Extensions, UAX#24);
     Common/Inherited (дефис/цифра/пунктуация) НЕ скрипт.
  P2 hold гейтится на CALLER-provided target; пользовательский текст НЕ задаёт targets (иначе атакующий
     куёт collision -> форсит hold). [SECURITY-стойка свода согласна]
  P3 полная запиненная UTS#39 = слой ДАННЫХ; курированный поднабор = АКТИВНАЯ политика.
  P4 разделители: канон = UTS#46/IDNA, не "NFKC вообще".
  P5 target-match = EXACT_LABEL, per-label, не по подстроке.

============================================================
§2. ATTACK-SIM НА ЖИВОМ ЯДРЕ (мои замеры 2026-08-04 — данные, бьют голос)
============================================================
SIM-1 (B2 ПОДТВЕРЖДЁН): xn--80ak6aa92e.com -> eff=pass, attn=NONE. Пуникод НЕ декодируется до детекта.
  Silent pass сохраняется ровно для формы, копипастимой из адресной строки. => IDNA-декод ДО детекта
  ОБЯЗАТЕЛЕН, иначе фронт слеп к самому распространённому носителю. Пин UTS#46 = 3-й версионируемый артефакт.
SIM-2 (B4 ИЗМЕРЕНО + УТОЧНЕНО): paypal．com (U+FF0E) И paypal。com (U+3002) в одиночку -> eff=pass,
  attn=NONE; report['text'] хранит СЫРОЙ вход (не подменён). _canon_domain_seps (sequence_engine.py:540)
  УЖЕ маппит ．。｡ -> '.' ЖЁСТКИМ dict (не runtime-NFKC -> P4 фактически соблюдён; комментарий про
  "NFKC" неточен, поведение верно), НО только для распознавания ФОРМЫ домена вокруг маски и БЕЗ порождения
  события. => подтверждает REDTEAM: канонизация тихая, до события. Standalone fullwidth-точка = silent pass.
SIM-3 (P1 — РЕШАЮЩЕЕ, не назвал НИКТО из 6): Python stdlib unicodedata НЕ имеет Script/Script_Extensions
  (dir->NONE; unidata_version=16.0.0). Ядро КАЖДОЙ леги по V2 ("resolved script set per label") НЕВОЗМОЖНО
  на stdlib -> нужна ВНЕШНЯЯ запиненная таблица Scripts.txt+ScriptExtensions.txt как артефакт репозитория
  (ver+sha256). Итог: ТРИ пиннутых артефакта (confusables + scripts/scriptext + UTS#46), не один.
SIM-4 (F2/P2 УЖЕ ФИЗИЧЕСКИ ГОТОВО): analyze(text, cards, protected_targets=None) — параметр УЖЕ есть,
  caller-supplied, .lower(), прокинут в O1Context(protected_targets, class138) (o1_policy_engine.py),
  exact-match; используется gate_c6_collapse_impersonation / gate_o1_primary_only. => двухъярусный hold
  (GPT two-level) НЕ новая проводка, а ПОВТОР паттерна D-O1-C6-NARROW. Security-модель P2 = уже канон проекта.
SIM-5 (host/label РЕАЛИЗУЕМ на существующей инфре): _looks_like_domain / _domain_prefix /
  _strip_label_invisibles / split('.') существуют -> per-label анализ строится поверх них. НО спека
  границы host-ТОКЕНА (userinfo p@evil.com / email local-part / видимый-текст-vs-href) РЕАЛЬНО отсутствует
  -> блокер B1 подтверждён как настоящая дыра, не гипотеза.

ЧТО СИМ ИЗМЕНИЛ В КАРТИНЕ ГОЛОСОВ:
  * Укрепил: host-only+per-label реализуемо (SIM-5); двухъярусный hold дешёв (SIM-4, проводка есть);
    IDNA-декод обязателен (SIM-1); таблицы = артефакты репозитория с sha256 (SIM-3 добавил 2-ю таблицу).
  * Добавил НОВЫЙ конструктивный блокер (SIM-3): нет script-свойства в stdlib -> Scripts.txt в репо.
  * Снял часть спора P4: dict уже жёсткий (не NFKC) -> "UTS#46 vs NFKC" в коде уже решён верно;
    остаётся ТОЛЬКО порядок "событие ДО канонизации" (B4) и куда повесить сигнал (F1).

============================================================
§3. РАЗВИЛКИ ДЛЯ АВТОРА (только то, что ГЕНУИННО открыто)
============================================================
F1. FULLWIDTH/IDEO-ТОЧКИ: (a) ОТДЕЛЬНАЯ ось parser-differential на DOT-пути [Gemini+Qwen+GPT+свод 3/4]
    vs (b) внутрь confusable-фронта через NFKC [Grok + FP-стойка]. SIM: канон уже есть, но МОЛЧА ->
    в любом случае нужно СОБЫТИЕ до канонизации. РЕКОМЕНДАЦИЯ коорд.: (a) отдельная ось + emit-event-before-canon.
F2. HOLD-ЯРУС СЕЙЧАС ИЛИ ПОТОМ: (a) включить caller-target hold в first-cut [GPT; проводка ГОТОВА, SIM-4]
    vs (b) first-cut = queue-only, hold отдельным инкрементом [большинство отложило hold]. РЕКОМЕНДАЦИЯ:
    (a) — почти бесплатно (реюз D-O1-C6-NARROW), и это единственный источник hold + ловец whole-script.
F3. 4 СПОРНЫХ МАППИНГА (ѡ→w η→n ա→a ս→u): (a) отложить с маркер-ячейками "не срабатывает" + реестр
    остатка [VERIFY+FP+REDTEAM 3/4] vs (b) включить [SECURITY-меньшинство: они в confusables.txt, witness
    переносит спорность на человека]. РЕКОМЕНДАЦИЯ: (a) — η частотна в науке (прямой FP-генератор).
F4. ИСТОЧНИК SKELETON: (a) UTS#39 pinned как ИСТОЧНИК + author-owned production-поднабор с diff-тестом
    против источника [синтез 3/4] vs (b) чисто ручной author-owned [REDTEAM]. РЕКОМЕНДАЦИЯ: (a).

============================================================
§4. БЛОКЕРЫ ДО КОДА (снять спекой в author decision; НЕ код)
============================================================
B1 СПЕКА host-токена: scheme/authority/userinfo, email local-part, видимый-текст vs href. [SIM-5 подтвердил дыру]
B2 IDNA/punycode-декод ДО детекта + пин UTS#46. [SIM-1 подтвердил silent pass на xn--]
B3 Пин ТРЁХ таблиц (confusables + scripts/scriptext + UTS#46) ver+sha256 + assert unidata_version=16.0.0;
   таблицы = артефакты репо, не рантайм-генерация. [SIM-3]
B4 Порядок пайплайна: ДЕТЕКТ -> СОБЫТИЕ -> канонизация. Тихий ремонт входа запрещён (witness).
   Переподписать §1-строку paypal．com (по UTS#46 это легит-канон paypal.com; замер pass — факт,
   ярлык "spoof" — интерпретация). [SIM-2]
B5 Батарея на FROZEN baseline: старый корпус побайтово + новые ячейки аддитивным отчётом; "не-confusable"
   определять ПЕРЕЧИСЛЕНИЕМ, не свойством "нет знаков из таблицы". Обязательные ячейки: см. свод §V5
   (позитивы pаypal/gоogle/gοogle/gօogle/Ⅽoinbase/paypal．com/paypal。com/смешанный-регистр/xn--после-декода;
   легит-контроли sber.рф/японский-IDN(Han+Kana)/корейский/дефисная-латиница/наука-в-прозе; блокер-ячейки
   alpha-testing.com-in-host->queue, alpha-user@lab.org->молчит, моно-скрипт-псевдобренд->не-срабатывает+реестр).
B6 Bump-политика пиннутых таблиц = явное author decision (кто/когда/с какой ре-валидацией).
B7 Witness-рендер: punycode/скрипт-аннотация — иначе "смотри глазами" пуст для истинных гомоглифов.
B8 РЕЕСТР ОСТАТКА (именованный, не скрытый): whole-script unlisted = ПОЛНЫЙ БАЙПАСС (ярлык, не "остаток");
   4 отложенных; наука-домены = остаточный queue-FP; вне-списочные бренды без яруса-2; ёмкость очереди
   (переполнение = фактический pass); r>g без корпуса — публиковать ИЗМЕРЕННЫЙ FP-набор, не численные претензии.

============================================================
§5. RANKED_OUTCOME + ВЕРДИКТ
============================================================
1. C two-tier класс-ось + host-гейт(спека B1) + IDNA-декод(B2) + per-label "латиница ∧ confusable-
   иноскрипт" (Common/Inherited-нейтр., CJK-исключения) + skeleton из pinned-UTS#39 (author-owned
   поднабор, diff-тест) + Scripts.txt pinned -> QUEUE. [единств. согласуется с обоими ARCH; реализуемо, SIM 3/4/5]
2. Списочный эскалатор поверх: skeleton(label) == skeleton(caller-target) ∧ confusable -> HOLD
   (реюз D-O1-C6-NARROW, проводка есть). Единственный источник hold + ловец whole-script.
3. B(81) — только nursery-сырьё после пересборки на token-уровень.
4. Per-host / голый ">=2 скрипта" — ОТКЛОНЕНО замером (FP sber.рф + CJK IDN).
5. Живой/непиннед UTS#39 или ручной без provenance — ОТКЛОНЕНО (тихая смена поведения / окно перебора).

ВЕРДИКТ: ПОПРАВИТЬ (не LOOPHOLE; host-контекст структурно отделяет адрес от прозы, полный C6/CONTEXT_V2
  не воспроизводится). Единственный настоящий LOOPHOLE-вектор — тихое расширение фронта на whole-script
  БЕЗ списка целей; запрещено ярлыком БАЙПАСС (B8).

FORK_STATUS: PENDING_AUTHOR_DECISION (F1-F4 + снятие блокеров B1-B8 спекой). Код заблокирован.
После decision: код + delta-census(frozen) + полная ре-валидация + маркер-ячейки реестра остатка.

END
