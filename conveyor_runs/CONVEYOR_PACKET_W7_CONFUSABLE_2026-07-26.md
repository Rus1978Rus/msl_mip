ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: CONVEYOR_PACKET_W7_CONFUSABLE_2026-07-26
DOCUMENT_TYPE: CONVEYOR_PACKET
PACKET_TYPE: DESIGN / PACKET_SUBTYPE: NEW_FRONT (видимые гомоглифы — mixed-script brand spoof)
DATE: 2026-07-26
PROJECT: MSL/MIP

РАМКА ПРОЕКТА: система ОПОВЕЩЕНИЯ, не антивирус. Машина — свидетель, не судья. Уровень —
  рекомендация человеку. Последнее слово ВСЕГДА за пользователем. Трёхуровневый сигнал:
  ЧИСТО(pass) / ВОЗМОЖНАЯ(queue_for_review) / РЕАЛЬНАЯ(hold_pending_review). Свидетель — отдельный
  канал «смотри глазами», НЕ вердикт.

ЗАДАЧА КРУГА: находка W7 (аудит Kimi + hole-hunt). ВИДИМЫЕ ДВОЙНИКИ — буквы, выглядящие как латиница
  (кириллица о/а/е/р/с/х…, греческие α/ο/ρ/…, армянские, римские цифры), и полноширинные разделители
  (．U+FF0E, 。U+3002) — маскируют бренд/домен. Основной проект их НЕ ловит: загружены 9 карточек
  (. / 💀 ☠ @ ／ ZWSP ZWJ BOM), confusable-карточек НЕТ. По ARCH_DECISION_HOMOGLYPH_VIA_CARD_ONLY
  двойник входит ТОЛЬКО через карточку — а их нет → тотальный silent pass. Судим ДИЗАЙН фронта ДО кода.

============================================================
§1. ФАКТ (измерено на живом ядре 2026-07-26 — данные, НЕ на голосование; спорить только замером)
============================================================
ТЕКУЩЕЕ ПОВЕДЕНИЕ, analyze():
  вход                                    semantic  effective  attention
  paypal.com (ASCII, контроль)              pass      pass       NONE
  gоogle.com (кириллическая о U+043E)       pass      pass       NONE   <- silent spoof
  pаypal.com (кириллическая а U+0430)       pass      pass       NONE   <- silent spoof
  paypal．com (fullwidth dot U+FF0E)        pass      pass       NONE   <- silent spoof
  sber.рф (чистая кириллица, легит IDN)      pass      pass       NONE   (легит)
  α-testing (греко-латинский science-compound) pass   pass       NONE   (легит)
ВЫВОД: ВСЕ видимые двойники сегодня → silent pass (даже не witness). Дыра тотальная. При этом легит
  (чистый скрипт-домен, научные составы) тоже pass — любой детектор ОБЯЗАН их НЕ будить.

СЫРЬЁ-КАНДИДАТ (Vakhter nursery, вычитано 2026-07-26; входит ТОЛЬКО через круг, не as-is):
  81 пер-знаковый confusable-маппинг (кириллица 36 / греч 26 / армян 5 / римские 14), сверены 1:1 с
  code-формой `confusable_cards.py`, маппинги корректны (UTS#39), ошибочных нет; 4 визуально спорных
  (ѡ→w, η→n, ա→a, ս→u). ИЗМЕРЕННЫЙ FP их ридера: срабатывает (conclusive ALARM) на легит греко-
  латинских science-составах `α-testing`/`β-cell`/`π-bond` (токенайзер включает '-'); их FP-тест
  гонял только одно-скриптовую русскую прозу — mixed-script FP не покрыл.
НЕИЗМЕРИМО: реальная частота homoglyph-атак vs легит-mixed-script в живом тексте — без корпуса честно
  не оценить.

============================================================
§2. ВОПРОС РЕВЬЮЕРАМ
============================================================
Выбрать ДИЗАЙН фронта видимых двойников так, чтобы: поднять spoof «латиница+двойник в домене» из
  silent pass; НЕ будить легит (чистый скрипт-домен `sber.рф`, научные составы `α-testing`, обычная
  не-латинская проза); держать witness-рамку; НЕ сломать батареи; честно назвать неустранимый остаток.
  По КАЖДОМУ V1–V6 — позиция + «где дыра».

============================================================
§3. ИЗМЕРЕНИЯ НА РЕВЬЮ
============================================================
V1. МЕХАНИЗМ. (A) mixed-script детект (латиница + confusable-буква иного скрипта в ОДНОМ host-токене)
    + UTS#39 skeleton/NFKC; (B) пер-знаковые confusable-карточки (81, Vakhter — медленно, но по
    ARCH_DECISION_HOMOGLYPH_VIA_CARD_ONLY); (C) КЛАССОВАЯ confusable-карточка/ось (двухъярусно, по
    ARCH_DECISION_TWO_TIER). Явный выбор с BASIS. Skeleton по запиненной таблице UTS#39 или ручной набор?
V2. FP-ГРАНИЦА (главное — тут ломается наивный mixed-script). Как НЕ будить: (i) чистый скрипт-домен
    (`sber.рф`, весь токен один не-латинский скрипт = легит IDN); (ii) science-составы `α-testing`
    (греко-латиница легитимна вне домена); (iii) не-латинскую прозу. Кандидаты: host-контекст-гейт
    (только в домене, не в прозе); whole-token-skeleton ∈ СПИСОК ЦЕЛЕЙ потребителя (C6-подобно);
    «≥2 скрипта в ОДНОМ label» vs «весь label один скрипт». Измерить каждый на §1-кейсах.
V3. СПИСОК ЦЕЛЕЙ (C6). Нужен ли потребительский brand/target-список, чтобы отличить spoof
    (`pаypal`→paypal ∈ список) от легит-mixed (`α-testing`∉список)? Или mixed-script-в-домене сам по
    себе достаточен? (Урок CONTEXT_V2/C6: без списка «проза/легит vs адрес» неразрешима локально.)
V4. УРОВЕНЬ + ОБЛАСТЬ. spoof → ВОЗМОЖНАЯ(queue, паритет с host-разрывами W4/W5/D-INV-GEN) или
    РЕАЛЬНАЯ(hold)? host/домен-only first-cut, или шире? Fullwidth/ideographic-точки (`paypal．com`,
    NFKC→'.') — в этот фронт или отдельно (DOT-карточка уже канонизирует их в reconstruction-пути)?
V5. БАТАРЕИ / DELTA-CENSUS. Confusable-детект не в ZWSP/ZWJ/BOM-батареях — подтвердить zero-delta для
    не-confusable входов; набор новых ячеек (spoof + легит-контроли: IDN/science/проза). Пин версии
    UTS#39/UCD.
V6. ОСТАТОК. 4 визуально спорных маппинга (ѡ→w, η→n, ա→a, ս→u) — включать или отложить? Полная таблица
    UTS#39 confusables vs курированный набор — цена/охват. Whole-script spoof (весь бренд в одном
    не-латинском скрипте, напр. all-Greek) — этот фронт или отдельно?
V-OTHER: если подход неверен (напр. это неразрешимо без списка целей = тот же корень, что C6/CONTEXT_V2)
    — сказать прямо (LOOPHOLE). Не смешивать с невидимками (W4/W5 — отдельные оси).

============================================================
§4. КРИТЕРИИ ОЦЕНКИ (по каждому измерению)
============================================================
(а) ПОДНИМАЕТ mixed-script spoof из silent pass; (б) НЕ БУДИТ легит (IDN/science/проза — ИЗМЕРЕНО,
  не «на глаз»); (в) r>g (охват без пропорц. FP; помнить измеренный α-testing FP); (г) WITNESS-РАМКА
  (рекомендация, монотонно); (д) НЕ ЛОМАЕТ верифицированное (батареи через delta-census); (е) ЧЕСТНОСТЬ
  (остаток, спорные маппинги, список-зависимость названы); (ж) РЕАЛИЗУЕМ (детерминированно; skeleton/
  таблица запинены).

============================================================
§5. ГРАНИЦЫ
============================================================
- НЕ пересматриваем: класс-138, три уровня, witness-рамку, ARCH_DECISION_HOMOGLYPH_VIA_CARD_ONLY
  (двойник входит через карточку/верифицированный путь), карточные пути.
- Сырьё Vakhter (81 маппинг + confusable_cards.py) — дизайн-кандидат, входит ТОЛЬКО через круг +
  author decision + ре-валидация, НЕ as-is (у ридера измерен science-compound FP).
- ЗАМЕРЫ §1 — данные; спорить только новым замером.
- КОД — только после author decision, с полной ре-валидацией.
- НЕ смешивать с невидимками (W4 VS / W5 whitespace — отдельные оси) и с CONTEXT_V2 (проза vs host —
  смежный корень, но отдельный фронт).

============================================================
§6. ДИСЦИПЛИНА (приложена)
============================================================
- kimi-methodological-audit-2026-07-26 (W7) · hole-hunt-2026-07-22 (homoglyph front открыт) ·
  ARCH_DECISION_HOMOGLYPH_VIA_CARD_ONLY (двойник через карточку) · ARCH_DECISION_20260726_TWO_TIER
  (класс vs знак) · vakhter-card-nursery (81 маппинг сверены 1:1, α-testing FP измерен) ·
  AUTHOR_DECISION_20260721_D-O1-C6-NARROW (collapse ∈ список целей — тот же target-list вопрос) ·
  DESIGN_NOTE_HOMOGLYPH_CONFUSABLE_FRONT · DESIGN_NOTE_E4_CONTEXT_V2 · RULE_DESIGN_ADVERSARIAL_SIM ·
  FOUNDATION_CONCEPT_PIKETTY_R_G.

============================================================
DELIVERABLE_FORMAT
============================================================
REVIEW_RESULT: REVIEWER / MODEL_FAMILY (семейств >=3, РАЗНЫХ). По КАЖДОМУ V1–V6 (+OTHER) —
  отметки (а)–(ж) + BASIS; для V1/V3/V4 — явный выбор; RANKED_OUTCOME; RECOMMENDATION; явно —
  дизайн верен / поправить / LOOPHOLE. НЕ голосовать §1 (факт); предлагать МЕХАНИЗМЫ, не голоса.
СЛЕПОТА: дизайн-круг; §1 (замер) — субстрат, дан законно.
EXIT: сходится → AUTHOR_DECISION → код + delta-census + полная ре-валидация; расходятся → материал
  автору; подход неверен → LOOPHOLE, пере-собрать.

FORK_STATUS: OPEN — PENDING_CONVEYOR + AUTHOR_DECISION (дизайн confusable-фронта). Код заблокирован.

END_OF_CONVEYOR_PACKET
