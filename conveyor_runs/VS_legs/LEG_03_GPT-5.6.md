VS-STEGO CONVEYOR LEG 03
REVIEWER: GPT-5.6 Thinking / MODEL_FAMILY: OpenAI GPT
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_VS_STEGO_2026-08-04
NOTE: внешняя сверка StandardizedVariants.txt 16.0.0, UTS#37 (IVD), emoji-variants, unicode.org/ivd/.

VERDICT: APPROVE_WITH_MAJOR_SPEC_PATCHES. дизайн верен, LOOPHOLE НЕТ. W4 СОХРАНИТЬ, её предикат
  молчания СУЗИТЬ, плагин ДОБАВИТЬ. Ключевая формула: ВАЛИДНАЯ БАЗА + ОДИН СЕЛЕКТОР != ВАЛИДНАЯ
  ПОСЛЕДОВАТЕЛЬНОСТЬ. Молчать допустимо только на ТОЧНО ЗАРЕГИСТРИРОВАННОЙ ПАРЕ, не на структурной форме.

POSITIONS:
  V1 SIGNAL     -> E: точная валидация ПАРЫ + сигнал на несанкционированную пару + байтовая проекция
                   (witness) + АГРЕГАТНЫЙ анализ носителя. Плотность CUT как ПЕРВИЧНЫЙ детектор
                   [строже Gemini, у которой она «запасной эвристик»]. Классы пары: STANDARDIZED_VALID /
                   EMOJI_VALID / IVD_VALID / UNSANCTIONED_PAIR / ORPHAN_SELECTOR / MULTI_SELECTOR_CHAIN /
                   UNVERIFIABLE. Сигнал НЕ зависит от длины текста, плотности, видимого прикрытия,
                   позиции и от того, декодируется ли нагрузка в слова => 120/600/10000 символов
                   прикрытия не выключают ось.
                   ***ЖЁСТКИЙ ACCEPTANCE-КРИТЕРИЙ (проверяемый):*** повторить 13/200/3000 байт; каждый
                   обязан дать QUEUE по причине UNSANCTIONED_PAIR (или авторизованному valid-pair
                   правилу), НЕ как побочный эффект плотности. ЕСЛИ 13-байтовая нагрузка окажется
                   состоящей ТОЛЬКО из санкционированных пар и останется PASS — дизайн V1 НЕ ЗАВЕРШЁН.
  V2 LEGIT      -> ***ТРИ ОФИЦИАЛЬНЫХ РЕЕСТРА, НЕ ДВА*** (фактическая поправка ко всем предыдущим легам
                   и к пакету): StandardizedVariants.txt + emoji-variation-sequences + IVD_Sequences.
                   Unicode санкционирует РОВНО три типа variation sequences; вне их селектор не задаёт
                   стандартного варианта отображения. CUT: «CJK + любой VS17..VS256», «emoji-подобная
                   база + FE0F», «графическая база + одиночный VS».
                   Судьба CJK+VS1 решается ТОЧНЫМ ПОИСКОМ ПАРЫ, а НЕ по диапазону: if pair ∈
                   StandardizedVariants -> valid; elif pair ∈ IVD -> valid; else UNSANCTIONED.
                   Легитимность emoji — по ТОЧНОМУ присутствию в таблице, не по свойству «похоже на эмодзи».
                   ***ПОПРАВКА О ПИНЕ (совпала с моим coord-флагом):*** IVD версионируется НЕЗАВИСИМО от
                   UCD, по ДАТЕ релиза; релизы кумулятивны и неизменяемы; GPT указывает, что на момент
                   ревью официальная страница уже перечисляет релиз 2026-08-03. Контракт пина:
                   UCD_VERSION 16.0.0 + EMOJI_DATA_VERSION 16.0 + IVD_RELEASE явной датой + три sha256 +
                   версия байт-декодера. Переход на новый IVD-релиз = отдельное авторское решение и
                   migration-delta, НЕ автоматический latest.
  V3 REVEAL     -> BINARY-SAFE структурное раскрытие: hex-preview + ЭКРАНИРОВАННЫЙ ASCII-preview +
                   ОПЦИОНАЛЬНЫЙ строгий UTF-8-preview (со статусом VALID/INVALID/INCOMPLETE) + полная
                   длина + sha256. [богаче Grok/Gemini: те давали только hex] Объект witness:
                   payload_byte_length, selector_count, original_spans, pair_status_histogram,
                   hex_preview, escaped_ascii_preview, strict_utf8_status/preview, preview_limit,
                   truncated, full_payload_sha256, decoder_id/version, artifact_versions.
                   N (preview limit) — implementation-константа с boundary-тестами, НЕ часть severity.
                   Допустимо сообщать STRICT_UTF8_VALID/INVALID; ЗАПРЕЩЕНО автоматически утверждать
                   EXECUTABLE / ENCRYPTED / MALWARE / COMMAND без отдельного анализатора и решения.
                   Запреты: не исполнять, не трактовать как разметку, не вставлять сырым в HTML,
                   не добавлять в LLM-промпт, не анализировать рекурсивно автоматически, не считать
                   именем файла/командой.
  V4 LEVEL      -> санкционированная одиночная emoji/IVD последовательность -> PASS, БЕЗ attention;
                   несанкционированная пара / сирота / невалидная цепочка -> MEDIUM/QUEUE;
                   измеренный CJK-стего-поток -> QUEUE + ОДИН агрегированный payload-witness;
                   host -> QUEUE по умолчанию, плагин НЕ создаёт ВТОРОЙ независимый вердикт
                   (base_level MEDIUM + vs_stego_level MEDIUM -> final MEDIUM + доп. witness);
                   HIGH/HOLD НЕ по умолчанию (covert-capable != proven malicious), только по отдельной
                   улике. ОБЪЁМ НЕ поднимает уровень (13 -> queue, 3000 -> queue): размер влияет на
                   witness/усечение/приоритет просмотра, но не доказывает злого смысла; 3000 может
                   дополнительно попасть под Input-Guard — это НЕЗАВИСИМАЯ DoS-ось.
                   ПОЛНОСТЬЮ ВАЛИДНЫЙ IVD-поток: first-cut сохраняет PASS (корпуса нет — честного порога
                   не установить), но плагин ОБЯЗАН уметь считать RESIDUAL_CARRIER_CAPACITY и держать
                   фронт ОТКРЫТЫМ, а не объявлять канал закрытым.
  V5 BATTERIES  -> нельзя просто обновить ожидания W4: нужен AUTHORIZED_VS_STEGO_DELTA_MANIFEST_v0_1,
                   по каждой ячейке CASE_ID / OLD_LEVEL / NEW_LEVEL / OLD_ATTENTION / NEW_ATTENTION /
                   EXPECTED_PAIR_STATUS / EXPECTED_PLUGIN_EVENT / ALLOWED_CHANGED_FIELDS /
                   AUTHOR_DECISION_ID. Позитивы: CJK 13/200/3000; CJK+VS1; CJK+произвольный VS17;
                   высокие значения селекторов; СМЕСЬ санкционированных и несанкционированных;
                   ОДНА несанкционированная пара среди длинного видимого прикрытия; нагрузка,
                   разбитая на несколько локальных прогонов; бинарь с 00/FF; валидный UTF-8;
                   невалидный UTF-8. Легит-контроли: 2764+FE0F, emoji+FE0E, emoji+FE0F, 845B+точная
                   зарегистрированная IVS, японская проза с РЕАЛЬНЫМИ IVS, много обычных
                   emoji-presentation последовательностей БЕЗ витнесс-флуда. Старые W4-кейсы (сирота,
                   цепочка, латиница+VS, невалидный emoji+VS17) остаются QUEUE; можно ДОБАВИТЬ
                   payload-witness, но НЕЛЬЗЯ тихо менять context/positions/старое основание вердикта.
                   Zero-delta по ZWSP/ZWJ/BOM/TAG/W5/W7/Input-Guard. MUTATION MANIFEST MUT-VS-01..14.
  V6 OSTATOK    -> ***ФОРМУЛА ОСТАТКА (строже Gemini):*** если у базы k зарегистрированных селекторов,
                   ёмкость ~= log2(k); если считать ОТСУТСТВИЕ селектора отдельным состоянием —
                   log2(k+1); по последовательности TOTAL_RESIDUAL ~= Σ log2(k_i + 1). Простейший
                   остаточный канал «селектор есть / селектора нет» = ~1 БИТ НА БАЗУ ДАЖЕ ПРИ ТОЧНОЙ
                   ВАЛИДАЦИИ. Отличить от настоящей типографики по одной строке принципиально трудно.
                   Также: несколько зарегистрированных вариантов одной базы — не теория (IVD хранит
                   десятки тысяч последовательностей); распределение между сообщениями (один селектор
                   на сообщение) обходит per-input агрегат — нужен будущий cross-message слой, вне
                   этого фронта; шифрованная/сжатая нагрузка: PAYLOAD DISCLOSED != PAYLOAD UNDERSTOOD.
                   Правильный статус: измеренный НЕСАНКЦИОНИРОВАННЫЙ канал — COVERED;
                   носитель на зарегистрированных вариантах — PARTIALLY COVERED / RESIDUAL;
                   всё скрытое содержимое — NOT_CLAIMED.
  V-OTHER-1     -> И ПРАВКА W4, И ПЛАГИН, с НЕПЕРЕСЕКАЮЩИМИСЯ обязанностями. W4: «что это за пара,
                   зарегистрирована ли, сирота ли, цепочка ли, корректен ли носитель». Плагин: «какой
                   поток байтов, где начинается/кончается, какова длина, как безопасно показать».
                   ОДИН общий индекс вхождений -> W4 -> плагин; ДВУХ полных сканов быть не должно.
  V-OTHER-2     -> FAIL-VISIBLE: отсутствие/несовпадение любой из трёх таблиц -> PAIR_STATUS=UNVERIFIABLE;
                   ЗАПРЕЩЕНО и «assume valid», и «assume invalid and hold everything»; first-cut =
                   один видимый системный witness + статус «анализ частично непроверяем»; «PASS без
                   оговорки» запрещён.
  V-OTHER-3     -> ПРОФИЛИ ВВОДА (GENERAL_TEXT / IVD_RICH_PUBLISHING_TEXT) для будущих агрегатных
                   порогов. Профиль НЕ МОЖЕТ: объявлять незарегистрированные пары легитимными,
                   выключать точную валидацию, назначаться ИЗ САМОГО АТАКУЮЩЕГО ТЕКСТА.
                   Несанкционированная пара — сигнал в ЛЮБОМ профиле.

CONVERGENCE (3 семейства: Grok+Gemini+GPT): таблица как главный рубильник, структурная форма CUT;
  зазор CJK+VS1 закрыть; hex-ориентированное бинарно-безопасное раскрытие + длина + sha256;
  queue-паритет, hold CUT, объём не поднимает уровень; молчание точного легита сохранить;
  авторизованный delta-манифест на W4-baseline; остаток «стего внутри зарегистрированных пар» назван
  всеми тремя независимо; W4 = носитель, плагин = нагрузка, один общий скан.
ПАТЧИ GPT СВЕРХ КОНСЕНСУСА (принять/проверить):
  P1 ТРИ реестра вместо двух (+StandardizedVariants) — фактическая поправка, никто не назвал.
  P2 Пин IVD ОТДЕЛЬНОЙ ДАТОЙ релиза (независим от UCD; указан релиз 2026-08-03) — совпало с моим флагом.
  P3 Точный поиск пары вместо диапазонной логики (CJK+VS1 решается таблицей, не диапазоном).
  P4 Остаток = Σ log2(k_i+1), >= 1 бит/база даже при точной валидации (строже «~1 бит» Gemini).
  P5 strict-UTF-8 статус в раскрытии + запрет автоматических ярлыков EXECUTABLE/ENCRYPTED/MALWARE.
  P6 Профили ввода с жёстким ограничением (не из атакующего текста, не отменяют точную валидацию).
  P7 Acceptance-критерий: если 13-байтовая нагрузка состоит только из санкционированных пар и остаётся
     PASS — дизайн не завершён.

COORD-FLAGS (проверить ЗАМЕРОМ на своде):
  * P7 — ПРЯМО ИЗМЕРИМО: взять §1-нагрузки и проверить по РЕАЛЬНОЙ таблице, сколько их пар
    санкционировано. Если хоть одна нагрузка целиком санкционирована — first-cut на одной валидации
    её НЕ ловит, и это надо знать ДО кода.
  * P1 — проверить, что StandardizedVariants.txt существует под 16.0.0 и его размер/содержание
    (третий артефакт => всего ШЕСТЬ пиннутых файлов в проекте).
  * P2/IVD — проверить фактический размер IVD_Sequences.txt и дату последнего релиза; сопоставить
    с нашим пином unidata 16.0.0 (assert в core/unicode_tables.py) — ложится ли отдельная дата.
  * P4 — ПОСЧИТАТЬ распределение k (числа зарегистрированных селекторов на базу) по реальной таблице:
    max/медиана/хвост и Σlog2(k+1) на типичной строке. Это проверяет и оценку Gemini «~1 бит», и
    формулу GPT. От результата зависит, насколько честно называть остаток.
  * Стоимость: три таблицы (IVD ~МБ, десятки тысяч пар) против 3-записного RGI-списка TAG-круга —
    измерить и взвесить r>g, включая время загрузки/память.
