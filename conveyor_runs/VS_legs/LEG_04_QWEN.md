VS-STEGO CONVEYOR LEG 04
REVIEWER: Qwen / MODEL_FAMILY: Alibaba
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_VS_STEGO_2026-08-04

VERDICT: APPROVE_WITH_AMENDMENTS. LOOPHOLE НЕТ. Две правки: IVD-таблица обязательна + плотность
  для валидного IVD.

POSITIONS:
  V1 SIGNAL     -> E = B(IVD-валидация) + A(плотность). Селектор на CJK-базе, не определённый в
                   таблице ДЛЯ ЭТОЙ БАЗЫ -> queue. Подтвердил структурное отличие от TAG (носитель
                   обязан быть смежен базе => плотность здесь надёжна) [3-я лега подряд].
                   Плотность = ВТОРИЧНЫЙ сигнал для стего ВНУТРИ валидного IVD.
                   ***ПОРОГ 50%*** доли «база+селектор» в окне. [РАСХОЖДЕНИЕ: Gemini назвала ~5%,
                   т.е. в 10 раз строже; Grok — «замерить»; GPT — плотность CUT как первичный детектор]
  V2 LEGIT      -> (ii) IVD-таблица + (i) emoji+FE0E/FE0F. (iii) структурная форма ОТКЛОНЕНА:
                   без таблицы ёмкость остаётся ~7.9 бит/знак, с таблицей падает до ~1-2 бит/знак.
                   ***СПОРНОЕ 1:*** «зазор CJK+VS1 закрывается АВТОМАТИЧЕСКИ, т.к. VS1 не входит в
                   IVD-диапазон E0100-E01EF» — рассуждение ПО ДИАПАЗОНУ, тогда как GPT (P3) требует
                   ТОЧНОГО ПОИСКА ПАРЫ и указывает, что санкционированных реестров ТРИ, включая
                   StandardizedVariants (где как раз живут пары с FE00). ПРОВЕРИТЬ ЗАМЕРОМ.
                   ***СПОРНОЕ 2:*** «эмодзи+FE0F — легит БЕЗ ТАБЛИЦЫ, FE0F единственный селектор для
                   emoji presentation» — GPT требует точного присутствия в emoji-variation-sequences
                   («не по свойству похоже-на-эмодзи»). ПРОВЕРИТЬ ЗАМЕРОМ: всякая ли база + FE0F валидна.
                   ***СПОРНОЕ 3:*** «IVD_Sequences.txt под UCD 16.0.0, доступен в UCD XML/ZIP,
                   обновляется с каждым релизом UCD» — ПРОТИВОРЕЧИТ GPT P2 (IVD = отдельный реестр
                   unicode.org/ivd/ со своей датой релиза). Уже ТРЕТЬЯ лега (Grok, Gemini, Qwen)
                   утверждает привязку к UCD; GPT один против трёх. ПРОВЕРИТЬ ЗАМЕРОМ — голосом не решать.
  V3 REVEAL     -> контракт TAG применим с бинарной адаптацией: попытка UTF-8 -> utf8_text либо
                   binary_data; формат hex(первые N)+...+hex(последние M) + length + sha256 + type;
                   кап N=50,M=50; конверт data-only, запрет авто-реинжеста сохраняются.
                   [хвостовой префикс «последние M байт» — новое, ни у кого не звучало]
  V4 LEVEL      -> queue везде (паритет TAG); host queue (паритет D-INV-GEN), hold только при ДРУГИХ
                   сигналах; ОБЪЁМ НЕ отличать на уровне вердикта (только в witness) [== все 4 леги];
                   молчание одиночного легит-эмодзи СОХРАНИТЬ (главный FP-источник W4, ради него D3).
  V5 BATTERIES  -> авторизованный delta-манифест (не тихий ре-базлайн); 9 ячеек с явными ожиданиями
                   (CJK-cover 13/200/3000 -> queue; emoji-cover 13 -> queue; CJK+VS1 -> queue;
                   ❤️ -> pass; японская IVD-проза -> pass; сирота/цепочка -> queue сохранены).
  V6 OSTATOK    -> стего внутри валидного IVD: ёмкость ~1-2 бит/знак, НЕ ноль; закрывать ПЛОТНОСТЬЮ
                   (>50% окна). Ярлыки остатка: VS_STEGO_RESIDUAL, VS_STEGO_INTERMESSAGE.
                   Прочие носители (ZW, interlinear) — отдельные оси, уже в реестре. Запрет
                   формулировки «сокрытие закрыто».
  V-OTHER       -> ВТОРОЙ ПЛАГИН поверх W4: W4 судит носитель, плагин судит нагрузку и МОЖЕТ ОТМЕНИТЬ
                   МОЛЧАНИЕ W4 (переопределить pass -> queue). [Формулировка «плагин отменяет молчание»
                   мягче, чем у GPT/Grok, которые правят САМ предикат молчания W4. Развилка формы.]

СЧЁТ ПО РАЗВИЛКАМ (4 семейства):
  D1 РЕЕСТРЫ:   два (IVD + emoji) = Grok, Gemini, Qwen (3)  |  ТРИ (+StandardizedVariants) = GPT (1).
                GPT привёл первоисточник; это ФАКТ, а не мнение -> проверяю замером.
  D2 ПИН IVD:   «под UCD 16.0.0» = Grok, Gemini, Qwen (3)  |  отдельная дата релиза = GPT (1).
                Тоже фактический вопрос -> замер.
  D3 ПЛОТНОСТЬ: первичный/вторичный сигнал = Grok, Gemini, Qwen (3, но пороги 5% vs 50% РАЗОШЛИСЬ
                в 10 раз)  |  CUT как первичный детектор, только агрегат после корпуса = GPT (1).
  D4 CJK+VS1:   «закроется само по диапазону» = Qwen  |  «только точным поиском пары» = GPT.
  D5 EMOJI:     «FE0F легит без таблицы» = Qwen (и косвенно Grok)  |  «только точная пара из таблицы»
                = GPT, Gemini.
  D6 ФОРМА:     править предикат молчания W4 = Grok, Gemini, GPT (3)  |  плагин переопределяет
                молчание сверху = Qwen (1).
ЕДИНОГЛАСНО (4/4): таблица обязательна, структурная форма CUT; бинарно-безопасное раскрытие
  (hex + длина + sha256 + тип), не угадывать текст; queue-паритет, hold CUT, ОБЪЁМ НЕ поднимает
  уровень; молчание точного легита сохранить; авторизованный delta-манифест; остаток «стего внутри
  зарегистрированных пар» назван всеми; W4 = носитель, плагин = нагрузка.
