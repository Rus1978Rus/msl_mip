VS-STEGO CONVEYOR LEG 02
REVIEWER: Gemini / MODEL_FAMILY: Google-Gemini
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_VS_STEGO_2026-08-04

VERDICT: дизайн ВЕРЕН, не LOOPHOLE. Плагин VS_STEGO в ось W4 = логичное развитие семейства.
  all (а)-(ж) PASS по всем осям.

POSITIONS:
  V1 SIGNAL     -> B (IVD-валидация по запиненной таблице) + плотность как ЗАПАСНОЙ эвристик.
                   Диагноз текущего правила: молчание слишком широкое -- пропускает ЛЮБОЙ селектор
                   E0100-диапазона на ЛЮБОЙ CJK-базе. Строгая валидация: молчать только если КОНКРЕТНАЯ
                   пара (base,selector) прописана в IVD_Sequences.txt. Плотностный триггер нужен ПОТОМУ,
                   что атакующий может остаться внутри легальных пар (V6): порог назван КОНКРЕТНО --
                   доля IVD-знаков > ~5% от всех CJK-знаков = аномалия.
                   ПОДТВЕРДИЛ структурное отличие от TAG: носитель привязан к базе, разбросать пробелами
                   нельзя => вычисление плотности здесь надёжнее, чем было бы у TAG. [== Grok V1]
  V2 LEGIT      -> (ii) IVD-таблица + (i) emoji+FE0E/FE0F ТОЖЕ из таблицы (emoji-variation-sequences).
                   (iii) структурная форма ОТКЛОНЕНА: «доказала несостоятельность в §1, породив дыру в
                   килобайты». 5-й артефакт оправдан r>g: закрывает БОЕВОЙ вектор (GlassWorm).
                   Зазор «CJK+VS1 (U+FE00)» закроется АВТОМАТИЧЕСКИ: IVD не использует FE00 => пара
                   становится нелегитимной. [== Grok V2, но Grok брал emoji-пары списком, Gemini -- тоже
                   таблицей => сходятся на ДВУХ таблицах]
  V3 REVEAL     -> контракт семейства применим с КРИТИЧЕСКОЙ адаптацией: прямой decode() в строку
                   НЕДОПУСТИМ (нагрузка бинарна). hex-дамп с ограничением (первые ~64 байта) + sha256
                   ВСЕЙ нагрузки. Конверт: covert_payload {type:VS_STEGO, raw_hex_prefix, full_sha256,
                   length_bytes}. Защищает и терминал человека, и LLM-потребителя от бинарного мусора.
                   [== Grok V3; Grok предлагал 'первых N байт', Gemini конкретизирует N~64]
  V4 LEVEL      -> паритет семейства: QUEUE везде. hold ОТКЛОНЁН с ДРУГИМ основанием, чем у Grok:
                   не «задняя дверь», а РИСК FP на нестандартных/устаревших CJK-системах, не
                   обновивших свои IVD (парализовать работу со старыми японскими шрифтами).
                   Молчание одиночного легит-эмодзи (включая прозу) ОБЯЗАНО сохраниться, иначе
                   возвращается witness-флуд.
  V5 BATTERIES  -> АВТОРИЗОВАННЫЙ манифест, документально фиксирующий изменение frozen baseline оси W4.
                   Векторы из §1: CJK-прикрытие 13/200/3000, сирота, легит ❤️, легит CJK+IVD.
                   Пин под UCD 16.0.0 критичен для детерминированности CI.
  V6 OSTATOK    -> ИМЕНОВАННЫЙ БАЙПАС **LEGAL_IVD_STEGO**: атакующий кодирует данные, используя ТОЛЬКО
                   пары, существующие в запиненной таблице. Оценка Gemini: у одного иероглифа «редко
                   больше 2-3 легитимных вариаций» => ёмкость падает с 8 бит/знак до ~1 бит/знак, но
                   канал ОСТАЁТСЯ. Честный неустранимый остаток; перевод из зиродея в «высокую
                   сложность эксплуатации» (нужен подбор текста-носителя с нужными иероглифами).
                   [== Grok V6 п.1, обе леги сошлись на этом остатке НЕЗАВИСИМО]

СКЕЛЕТ РЕАЛИЗАЦИИ (Gemini): 1) парсер IVD_Sequences.txt + emoji-variation-sequences.txt в сборку ->
  статический frozenset валидных пар; 2) правило W4: if (base,vs) not in VALID_PAIRS -> QUEUE;
  3) для ВСЕХ селекторов (включая валидные) запустить накопитель нагрузки; 4) если есть хотя бы один
  невалидный ИЛИ плотность > T (~5%) -> hex-префикс + sha256, effective_action=QUEUE,
  reason=VS_STEGO_CHANNEL; 5) AUTHOR_DECISION_MANIFEST на изменение frozen baseline.

CONVERGENCE с LEG_01 (2 семейства): V1 = таблица как главный рубильник + плотность как усилитель;
  V2 = табличный легит, структурная форма CUT, зазор CJK+VS1 закрыть; V3 = hex + длина + sha256,
  не угадывать текст; V4 = queue, hold CUT, молчание эмодзи сохранить; V5 = авторизованный
  delta-манифест на W4-baseline; V6 = LEGAL_IVD_STEGO как названный остаток ~1 бит/знак.
  РАЗНИЦА В ОСНОВАНИИ hold-CUT: Grok — «задняя дверь для hold», Gemini — «FP на старых CJK-системах».
  Оба довода независимы и совместимы.

COORD-FLAGS (проверить ЗАМЕРОМ на своде):
  * ВЕРСИЯ (повтор флага из LEG_01, теперь ДВЕ леги подряд): обе пишут «IVD под UCD 16.0.0», но
    IVD_Sequences.txt -- ОТДЕЛЬНЫЙ реестр Unicode (unicode.org/ivd/) со СВОЕЙ датой ревизии, НЕ часть
    UCD. Проверить, что пинится и как ложится на assert unidata_version.
  * НОВОЕ ИЗМЕРИМОЕ УТВЕРЖДЕНИЕ Gemini: «у иероглифа редко больше 2-3 вариаций» => ~1 бит/знак.
    ПРОВЕРИТЬ СЧЁТОМ по реальной таблице: распределение числа вариантов на базу (max/медиана/хвост).
    Если у части баз вариантов много (Adobe-Japan1), остаточный канал ВЫШЕ 1 бита -- и тогда
    LEGAL_IVD_STEGO надо назвать честнее.
  * ПОРОГ 5%: у Gemini конкретен, у Grok -- «замерить на японской прозе». Корпуса японской IVD-прозы
    у нас НЕТ (§1 НЕИЗМЕРИМО) => порог непроверяем. Взвесить, не убрать ли плотность из first-cut
    (только таблица), чтобы не вводить неизмеренный параметр -- ИЛИ ввести её только как
    escalate-from-silence с явной пометкой «параметр не откалиброван».
  * ЦЕНА АРТЕФАКТА: обе леги называют таблицу дешёвой; ИЗМЕРИТЬ реальный размер IVD (десятки тысяч
    последовательностей, ~МБ) против 3-записного RGI-списка TAG-круга.
  * Пункт 3 скелета Gemini («накопитель нагрузки для ВСЕХ селекторов, включая валидные») -- проверить
    стоимость: это декод ВСЕГДА, а Grok явно отверг D-как-сигнал; как объект раскрытия допустимо,
    но надо убедиться, что не создаёт шум на легит-IVD-прозе.
