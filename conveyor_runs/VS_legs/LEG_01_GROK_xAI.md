VS-STEGO CONVEYOR LEG 01
REVIEWER: Grok / MODEL_FAMILY: xAI-Grok
RECEIVED: 2026-08-04
PACKET: CONVEYOR_PACKET_VS_STEGO_2026-08-04

VERDICT: дизайн верен (как ПРАВКА молчания W4 + ПЛАГИН семейства). LOOPHOLE НЕ подтверждён.
  Дыра = прямое следствие сознательного D3, закрывается сужением whitelist без возврата флуда.

POSITIONS:
  V1 SIGNAL     -> E = B(валидация пары по запиненной таблице) + A(покрытие баз-с-селектором в окне)
                   как УСИЛИТЕЛЬ. Сигнал first-cut: (база+селектор) ∉ pinned_legit_pairs ИЛИ доля
                   баз-с-селектором в окне >= tau. C(энтропия) — усилитель ПОЗЖЕ. D(декод всегда) —
                   для РАСКРЫТИЯ, не как единственный сигнал (иначе легит-IVD со «случайными» байтами
                   = шум). Урок TAG учтён: плотность НЕ главный рубильник; структурное отличие
                   подтверждено — носитель ОБЯЗАН быть смежен базе, фрагментация как у TAG невозможна.
                   Дыра: tau и окно надо ЗАМЕРИТЬ на японской прозе с реальными IVD; до замера tau
                   держать консервативно и только как escalate-from-silence.
  V2 LEGIT      -> (i)+(ii): молчать ТОЛЬКО если (emoji_base + FE0E/FE0F) ИЛИ (base,VS) ∈ pinned
                   IVD_Sequences. (iii) структурная форма без таблицы ОТКЛОНЕНА — именно она и
                   образует измеренный канал. Зазор §1 «CJK+VS1 (U+FE00)»: VS1 вне IVD-диапазона =>
                   НЕ легит-пара => побочный зазор правила, first-cut обязан закрыть.
                   Арифметика ёмкости ВНУТРИ легита: emoji+FE0E/FE0F ~= 1 бит (выбор стиля);
                   IVD — вариантов на базу обычно мало (1..few) => бит/знак << 8; байтовый алфавит
                   внутри строгого IVD-whitelist почти схлопывается.
  V3 REVEAL     -> контракт семейства применим, НО НЕ дословно: нагрузка = ПРОИЗВОЛЬНЫЕ БАЙТЫ.
                   hex (или base64) первых N байт + len + sha256 + payload_kind=binary|text;
                   НЕ угадывать UTF-8 «на глаз»; конверт data-only и запрет авто-реинжеста — те же.
  V4 LEVEL      -> паритет семейства: queue. Одиночный emoji+FE0F / валидный IVD -> pass (молчание
                   СОХРАНЕНО). Пара ∉ pinned -> queue + decode. Аномальное покрытие -> queue.
                   Host -> queue (паритет D-INV-GEN), НЕ hold. Объём (13 vs 3000) -> в witness-meta
                   (payload_len), НЕ автоматический hold: иначе hold возвращается ЗАДНЕЙ ДВЕРЬЮ
                   вопреки дисциплине семейства.
  V5 BATTERIES  -> АВТОРИЗОВАННЫЙ delta-манифест на каждое изменение правила молчания W4 (не тихий
                   ре-базлайн). Ячейки: CJK-cover 13/200/3000 -> queue; emoji-cover стего; CJK+VS1 ->
                   queue; ❤️ -> pass; легит IVD -> pass; японская проза с реальными IVD -> pass;
                   сирота/цепочка без регрессии W4. Пин IVD/emoji-VS под unidata 16.0.0, проверить
                   наличие в PIN_MANIFEST ДО кода.
  V6 OSTATOK    -> не закрывается: стего ВНУТРИ валидного IVD (выбор среди реально определённых
                   вариантов; бит/знак << 8, но НЕ ноль); ZW-схемы и interlinear (уже в реестре);
                   нагрузка между сообщениями. Запрет формулировок «сокрытие закрыто» / «VS-стего
                   закрыто».
  V-OTHER       -> НЕ отмена W4 и не LOOPHOLE. Архитектура: W4-VS судит НОСИТЕЛЬ; VS-STEGO = ВТОРОЙ
                   ПЛАГИН семейства поверх неё, судит НАГРУЗКУ, когда носитель ВЫГЛЯДИТ легитимно.
                   Правка W4 МИНИМАЛЬНА (сузить белый список до pinned pairs), ценность — в плагине.

RANKED: 1) сузить молчание до pinned emoji/IVD + плагин стего  2) только плотность без таблицы CUT
        3) декод всегда без легит-гейта CUT  4) hold на объём CUT (задняя дверь)
        5) оставить безусловное молчание одиночки CUT (измеренный канал).

COORD-FLAGS (проверить ЗАМЕРОМ на своде, не на слово):
  * ГЛАВНОЕ: IVD_Sequences.txt — ОТДЕЛЬНЫЙ реестр Unicode (unicode.org/ivd/) со СВОЕЙ версионностью,
    он НЕ часть UCD 16.0.0. Grok дважды говорит «пин под UCD 16.0.0» — проверить, что это вообще
    возможно, и как согласуется с нашим assert unidata_version.
  * ЦЕНА АРТЕФАКТА: Grok зовёт таблицу «конечной, цена оправдана», но IVD — это ДЕСЯТКИ ТЫСЯЧ
    последовательностей (Adobe-Japan1 + Hanyo-Denshi + Moji_Joho + KRName), не 3 записи как RGI.
    ИЗМЕРИТЬ размер и посчитать ёмкость канала ВНУТРИ легита (арифметика бит/знак, как SIM-5 в TAG):
    утверждение «бит/знак << 8» надо ПОДТВЕРДИТЬ счётом вариантов на базу, а не принять на слово.
  * tau/окно — Grok сам признал, что нужен замер на японской IVD-прозе; корпуса у нас НЕТ (§1
    НЕИЗМЕРИМО). Значит A-плотность в first-cut = непроверяемый параметр => взвесить, не выкинуть ли
    её из first-cut целиком (оставив только B), чтобы не вводить неизмеренный порог.
  * ПРОВЕРИТЬ на живом ядре: действительно ли emoji+FE0E/FE0F даёт ~1 бит, и не молчат ли ещё
    какие-то пары помимо найденных (полная карта база×селектор, а не точечные пробы).
