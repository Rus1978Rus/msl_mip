ЧАСТНЫЙ АВТОРСКИЙ ПРОЕКТ / COMMERCIAL USE PROHIBITED

DOCUMENT_ID: HANDOFF_2026-07-15
DOCUMENT_TYPE: SESSION_HANDOFF (пакет передачи новому чату — снимок состояния)
DATE: 2026-07-15
AUTHOR: Руслан Малявский
PROJECT: MSL/MIP — Malyavsky Syntax Language / Malyavsky Invariant Protocol

====================================================================
0. РАМКА ПРОЕКТА (держать в силе ВСЕГДА, дословно)
====================================================================
Это НЕ антивирус. Это СИСТЕМА ОПОВЕЩЕНИЯ. Машина не судья и не палач — она
свидетель. Последнее слово ВСЕГДА за пользователем.
- Не «заблокировать/удалить/защитить» → «распознать, сообщить, вынести на глаза».
- Вердикт (PASS/QUEUE/HOLD) — это РЕКОМЕНДАЦИЯ человеку, не команда системе.
Метафора: знаки — грим на юникоде; мимикрия = чужое лицо в знакомом гриме.

ДИСЦИПЛИНА РАБОТЫ (из памяти проекта):
- точные счётчики в статусах (не «около»); показывать и ЛОЖНЫЕ СРАБАТЫВАНИЯ, не
  только атаки; карточка+код едут на конвейер одним пакетом;
- проверять ЗАПУСКОМ, не чтением; «не рассуждать — считать»;
- сверять против КАРТОЧЕК (первоисточник), а не мнений ревьюеров;
- в .py комментариях только ASCII (кириллица в комментах валит english-only гейт).

====================================================================
1. ГДЕ СЕЙЧАС (git)
====================================================================
Ветка: main. HEAD = origin/main = 45986d1 (по данным git оба последних коммита
на origin — запушены).
Последние коммиты:
  45986d1  ZWSP -> WORKINGLY_CLOSED_PENDING_CLASS_GUARD (author decision)
  b8ebfa9  ZWSP card doc-sync: 21/21 everywhere, RUNTIME_REALITY aligned, 12/12
  c7a88e9  ZWSP external review packet (from fresh card) + remove stale packet
  1939f15  ZWSP preflight pass: mutation + phago N/A + review-format open node
  b937911  F-NEW-4 URL component (QUERY/FRAGMENT) + F-NEW-5 userinfo; battery 21/21

Рабочее дерево ЧИСТОЕ, КРОМЕ этого хендофф-файла (и, возможно, session-scratchpad
скриптов — они вне git).

ENV-КВИРКИ (Windows, из памяти):
- SAC блокирует git push по HTTPS (err 4551) → пуш через GitHub Desktop;
- использовать `py -3`, НЕ `python`; Bash-tool на этой машине сломан → PowerShell;
- батарею гнать с env: MSL_MIP_HERMETIC_TLD=1, PYTHONUTF8=1, PYTHONIOENCODING=utf-8.

====================================================================
2. КАРТОЧКА ZWSP — ТЕКУЩИЙ СТАТУС (главный артефакт сессии)
====================================================================
Файл: cards/SIGN_CORE_CARD_ZERO_WIDTH_SPACE_U200B_GEN3_v0_3_RU.md
CODEPOINT: U+200B. Первая карточка класса НЕВИДИМЫХ знаков → станет ШАБЛОНОМ для
ZWNJ/ZWJ/BOM/…

СТАТУС (поднят сегодня, AUTHOR_DECISION D-ZWSP-WORKINGLY-CLOSED 2026-07-15):
- DOCUMENT_STATUS: WORKINGLY_CLOSED  (машинное поле — код-гейт; поднято с
  WORKING_DRAFT, т.к. пройдены preflight+conveyor+author-decision = ровно
  определение _VALID_STATUSES; предупреждение CARD_NOT_CONVEYOR_REVIEWED
  корректно погашено, было бы ложным);
- LIFECYCLE_STATUS: WORKINGLY_CLOSED_PENDING_CLASS_GUARD (квалификатор
  _PENDING_CLASS_GUARD живёт ЗДЕСЬ — код такой строки не знает, в DOCUMENT_STATUS
  был бы CARD_INVALID).

КОД-ГЕЙТ (знать, чтобы не сломать):
  single_sign/module_engine.py:69  _VALID_STATUSES = {WORKINGLY_CLOSED, ARTIFACT_CONFIRMED}
  single_sign/module_engine.py:70  _DRAFT_STATUSES = {WORKING_DRAFT}  -> печатает CARD_NOT_CONVEYOR_REVIEWED
  sequence/sequence_engine.py:816  _CONFIRMED_STATUSES = {WORKINGLY_CLOSED, ARTIFACT_CONFIRMED}
  Строка "WORKINGLY_CLOSED_PENDING_CLASS_GUARD" коду НЕизвестна → только в LIFECYCLE_STATUS.

ДОКАЗАНО ЗАПУСКОМ (всё сошлось):
- симуляция BY_CODE 21/21 (registrar 6/6, positions 4/4, combo 2/2, url 2/2,
  dangerous 5/5, controls 2/2), двуногая, mutation-adequacy 5/5 killed, baseline
  стабилен; после подъёма статуса warning-флуд ZWSP -> 0 (флип вступил в силу);
- preflight 35 PASS / 0 FAIL / 1 PRECEDENT (CONFUSABLES-арбитраж класса невидимых);
- внешнее конвейер-ревью 8/8 ACCEPT, PASS_WITH_PATCHES (судился ДОКУМЕНТ, не код);
- BY_CODE-сверка карта<->код согласованы (obещает-не-производит: []; код-впереди-
  карты: []); наводки MS Copilot (EMAIL/HOLD, QUERY вне scope) НЕ подтвердились;
- doc-sync: 12/12 внутренней консистентности, карточка не противоречит сама себе.

PATH_TO_ARTIFACT:
  1. preflight + conveyor review — ПРОЙДЕНЫ.
  2. WORKINGLY_CLOSED_PENDING_CLASS_GUARD — ДОСТИГНУТО (сегодня).
  3. построить INVISIBLE_DEFAULT_IGNORABLE_GUARD (из >=3 разных невидимых) +
     ре-валидация ВСЕХ карточек класса   <-- СЛЕДУЮЩИЙ ШАГ (CLASS_FRONT, не блокер).
  4. ARTIFACT_CONFIRMED  (blocked ТОЛЬКО классовым гардом — не дефект карточки).

RELATION-ОСЬ карточки (для справки):
  SIGN_RELATIONS: BOUNDARY_DISRUPTOR=PRIMARY, ABSENCE_CONFUSABLE=SUPPORTING_FACET,
  INVISIBLE_CLASS_COLLISION=TAXONOMY_ONLY.
  CONTEXT_SCOPE (BOUNDARY_DISRUPTOR) = HOST, EMAIL, PATH, BYTE_EXACT_TOKEN,
  QUERY_VALUE, FRAGMENT, USERINFO, HIDDEN_BOUNDARY_PADDING (риски: HOST->HIGH,
  прочие->MEDIUM, FREE_TEXT->NONE). Детектор — _detect_context_at в
  sequence/sequence_engine.py (Pass1 scheme-ветка authority/path/QUERY/FRAGMENT +
  userinfo по последнему @; Pass2 bare-domain/EMAIL).
  PHAGO: NOT_APPLICABLE, VERIFIED (D-ZWSP-STATUS Q1; конвейер 9 рев. 8:1 + author
  decision; CATEGORY_E активно доказан). Критерий — RULE_PHAGO_APPLICABILITY_v0_1.md.

====================================================================
3. AUTHOR_DECISIONS и OPEN_NODES (foundation_layer/)
====================================================================
[ПОЛНЫЙ ДАМП foundation_layer — в парном файле HANDOFF_FOUNDATION_LAYER_2026-07-15.md
 (39 файлов целиком, оглавление + статус-теги; 8 помечены CURRENT/LOAD-BEARING).
 Ниже — только самое актуальное; за полным текстом идти в дамп или в git.]
РЕШЕНИЯ по ZWSP:
- AUTHOR_DECISION_20260713_D-ZWSP-STATUS.md — Q1 фаго N/A; Q2 классовый гард не
  блокер (CLASS_FRONT); Q3 держать до WORKINGLY_CLOSED (ревью не пройдено).
- AUTHOR_DECISION_20260715_D-ZWSP-WORKINGLY-CLOSED.md — подъём до
  WORKINGLY_CLOSED_PENDING_CLASS_GUARD (сегодня; шаг 2 закрыт).

ОТКРЫТЫЕ УЗЛЫ (нерешённые, зафиксированы честно):
- OPEN_NODE_CARD_SINGLE_SOURCE_OF_TRUTH.md — один факт размазан по 5-6 разделам
  карточки без единого источника -> рассинхрон; кандидат в канон «один факт —
  один источник» (либо гейт-сверка копий). Структуру шаблона НЕ трогать сейчас.
- OPEN_NODE_CONVEYOR_REVIEW_FORMAT.md — формат конвейер-ревью устарел (сложился
  до двуногой симуляции); ZWSP-проход стал прецедентом нового формата.
- OPEN_NODE_PHAGO_NATURE.md — гипотеза фагоцитоза; природа PHAGO как процесса.
- CLASS_FRONT_INVISIBLE_SIGNS.md — классовая зависимость (гард из >=3 невидимых).
- RULE_PHAGO_APPLICABILITY_v0_1.md — каноничный критерий PHAGO (APPLICABLE <=>
  знак СВОЕЙ функцией создаёт ложную принадлежность; / APPLICABLE, . и ZWSP
  NOT_APPLICABLE).

====================================================================
4. ПОСЛЕДНЯЯ РАБОТА (НЕ закоммичена) — СИМУЛЯЦИЯ КРИТЕРИЯ «ФУНКЦ. СОСЕДИ»
====================================================================
СТАТУС: прогнано запросом, НЕ внесено в шаблон, карточки НЕ тронуты. Результат
ждёт авторского решения. Скрипт — в Приложении A (session-scratchpad эфемерен,
поэтому текст сохранён здесь целиком; можно перепрогнать `py -3`).

ПРАВИЛО (из конвейера, словами):
- знак под правилом, если НЕ производит видимого глифа (наблюдаемый эффект, не
  формальная категория Unicode);
- ФУНКЦИОНАЛЬНЫЙ СОСЕД = знак, НЕРАЗЛИЧИМЫЙ ДЛЯ НАИВНОГО ФИЛЬТРА (оба невидимы/
  игнорируемы при грубой обработке). НЕ «похожий по функции»;
- перечислять ВСЕХ; число НЕ фиксируется; добивать до 5 мусором ЗАПРЕЩЕНО;
- пограничные — помечать явно.

ЧТО ПОКАЗАЛ ПРОГОН (Unicode 16.0, критерий = DI + General_Category + Bidi_Class +
advance-width):
(A) Под-правило-вердикты чистые: ZWSP/BOM/ZWJ/ZWNJ -> YES; SHY -> YES/погранично
    (дефис при переносе); BRAILLE BLANK U+2800 -> NO/погранично (So, видимый, но
    пустой рендер — ДРУГАЯ ось); NBSP U+00A0 -> NO (Zs, видимый пробел — ось
    визуальных двойников, не соседей); SPACE/CYRILLIC а/LATIN a -> NO (стражи держат).
(B) РАСХОЖДЕНИЕ КАРТОЧКА<->КРИТЕРИЙ (главное):
    - КАРТОЧКА (5): U+00AD, U+200C, U+200D, U+2060, U+FEFF.
    - КРИТЕРИЙ (tightest text-bucket, Cf+BN+DI): 22. Strict (+directional+deprecated): 40.
    - Карточка ⊂ критерий (0 лишних в карточке — все 5 реальные).
    - Критерий находит ЕЩЁ 17, которых в карточке НЕТ: U+2061..2064 (невидимые
      матоператоры FUNCTION APPLICATION / INVISIBLE TIMES/SEPARATOR/PLUS), U+180E
      (MONGOLIAN VOWEL SEPARATOR), U+1BCA0..1BCA3 (shorthand), U+1D173..1D17A (musical).
    - Ни одно UCD-свойство не отделяет U+2060 (в карточке) от смежного U+2061 (нет).
(C) УДАР ПО КОНСИСТЕНТНОСТИ: U+2062/U+2063 УЖЕ в собственной BY_CODE-батарее
    (кейс K3: goog<U+2062>le.com -> HOST/HIGH). Движок уже трактует их как
    невидимых соседей ZWSP, а карточка их не включает — против своего же рантайма.
(D) ОТВЯЗКА ОТ «5»: реальное число = 22 (или 40), а не 5. Карточкины «5» совпадают
    со старой ОТМЕНЁННОЙ квотой CONFUSABLES>=5 — похоже на подгонку под квоту.
(E) ГДЕ ЛОМАЕТСЯ: «наивный фильтр» недоопределён. Одна проза -> >=4 разных
    property-предиката -> 5 / 22 / 40 / +263 attaching. Пул assigned DI по бакетам:
    ZW_TEXT_FMT:23, DIRECTIONAL_FMT:12, DEPRECATED_FMT:6, ATTACHING:263,
    FILLER_WIDTH:4, TAG:97.

ВЕРДИКТ (честно): неправы ОБА.
- Критерий — недоспецифицирован: «наивный фильтр» надо пришпилить к явному
  UCD-предикату (напр. DI ∧ gc=Cf ∧ bidi=BN ∧ ¬tag ∧ ¬deprecated -> тогда для
  ZWSP это 22, не 5).
- Карточка — недобирает по критерию И её «5» отобраны ПО ФУНКЦИИ (оставили
  текстовые форматы, выкинули мат/ноты/скоропись), а правило ПРЯМО это запрещает
  («НЕ похожий по функции»). Т.е. карточка применила ровно тот функциональный
  фильтр, который правило отвергает.
РЕШИТЬ ДО ВНЕСЕНИЯ В ШАБЛОН.

====================================================================
5. ОТКРЫТЫЙ ФРОНТ (не начат) — INPUT GUARDS
====================================================================
Черновик: foundation_layer/drafts/DRAFT_INPUT_GUARDS_2026-07-13.md
Из памяти (input-guards-front): байтовый якорь + DoS через невидимую
комбинаторную детонацию; байтовый лимит (mode A) — следующий после ZWSP;
file-input = крупный ОТДЕЛЬНЫЙ фронт.

====================================================================
6. ЧТО ДЕЛАТЬ НОВОМУ ЧАТУ (краткий next-actions)
====================================================================
- Прежде чем что-либо менять: перечитать первоисточники (карточка + RULE-файлы +
  AUTHOR_DECISION), не по памяти. Проверять ЗАПУСКОМ.
- Открытое решение №1: критерий «функц. соседи» (см. раздел 4) — либо пришпилить
  предикат и принять 22 для ZWSP, либо авторски сузить и обосновать СВОЙСТВОМ, не
  функцией. Только после этого — в шаблон.
- Артефакт ZWSP заблокирован ТОЛЬКО классовым гардом (нужно >=3 невидимых знака),
  это CLASS_FRONT, не дефект. Следующая карточка класса (ZWNJ/ZWJ/BOM) двигает и гард.
- Ничего не коммитить/пушить без явной команды автора. Пуш — через GitHub Desktop.

====================================================================
ПРИЛОЖЕНИЕ A — скрипт критерия «функц. соседи» (перепрогоняемый: py -3)
====================================================================
СКРИПТ УЕХАЛ В ПАКЕТЕ: neighbors_criterion_2026-07-15.py (в корне проекта).
 Запуск: PYTHONUTF8=1 PYTHONIOENCODING=utf-8 py -3 neighbors_criterion_2026-07-15.py
 Зависит только от stdlib (sys, unicodedata). DI_RANGES — из UCD
 DerivedCoreProperties (Default_Ignorable_Code_Point). Логика: props(cp) ->
 gc/bidi/DI; visibility() -> INVISIBLE/INVISIBLE_DIRECTIONAL/BORDERLINE/VISIBLE;
 under_rule(); POOL = все assigned DI; bucket_of() -> ZW_TEXT_FMT/DIRECTIONAL_FMT/
 DEPRECATED_FMT/ATTACHING/FILLER_WIDTH/TAG; neighbors(target) -> (strict, text);
 сверка с CARD_ZWSP = {200C,200D,2060,FEFF,00AD}.
 (Файл НЕ в git — часть пакета передачи; коммитить только по решению автора.)

END_OF_HANDOFF
